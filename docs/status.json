{
  "$comment": "Single source of truth for the SimWorld build tracker. Edit here; tools/blueprint regenerates the page. Item ids are stable keys — the tracker persists human ticks against them.",
  "project": "SimWorld",
  "updated": "2026-09-11",
  "testsTotal": 1775,
  "stack": {
    "language": "C#",
    "host": "Unity (renderer / UI host, added when there is something to render)",
    "core": "src/SimWorld.Core — netstandard2.1 class library, no UnityEngine references, consumable as a local UPM package",
    "tests": "xUnit via dotnet test (tests/SimWorld.Core.Tests)",
    "ci": "GitHub Actions: dotnet build+test, blueprint build, markdown lint, link check, mermaid validation"
  },
  "phase": {
    "id": 1,
    "name": "Systems as isolated modules",
    "summary": "Port each RimWorld system as a separately tested module. No game loop, no renderer, no god layer yet.",
    "launch": {
      "available": false,
      "reason": "Nothing to launch yet — phase 1 ships modules and tests only. A launch link appears here once a playable loop exists."
    }
  },
  "statusLegend": {
    "planned": "Not started",
    "in-progress": "Being ported",
    "ported": "RimWorld behaviour implemented and tested",
    "translated": "Re-targeted for the god-game on top of the port",
    "verified": "Exercised in a playable build"
  },
  "decisions": [
    {
      "id": "lang",
      "date": "2026-09-06",
      "state": "decided",
      "title": "C# with Unity as the host",
      "note": "Core is an engine-free netstandard2.1 library (C# 9) so it builds and tests in CI without Unity; Unity references it as a local package."
    },
    {
      "id": "scope",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Step 1 = isolated modules, no game loop",
      "note": "Each of the 17 researched systems becomes a module with its own tests before any loop or renderer."
    },
    {
      "id": "depth",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Every citizen is a full agent",
      "note": "RimWorld-depth needs/mood/health/skills/jobs per person at civilization scale. Population target: as large as we can; the performance ceiling is a later bridge."
    },
    {
      "id": "tech",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Large authored tech tree of societal evolution",
      "note": "Endless/procedural tech comes later; the research module's data model must not preclude it."
    },
    {
      "id": "director",
      "date": "2026-09-06",
      "state": "open",
      "title": "Director persona name",
      "note": "RimWorld's storyteller is translated into a chronicle-style narrator targeting YOUR civilization. Working candidates: Scribe, Historian, The Chronicle. Code module stays 'Director' until chosen."
    },
    {
      "id": "launch",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Launch only when it makes sense",
      "note": "Tracker shows launch disabled with the reason until a playable loop exists."
    },
    {
      "id": "tracker",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Living tracker",
      "note": "Third tab in the Blueprint, generated from docs/status.json; repo-verified items are locked, remaining items are tick-able and persisted for everyone who opens the page."
    },
    {
      "id": "dupes",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Duplicate defName policy: later pack wins",
      "note": "Implementation choice mirroring mod load-order semantics (DefLoadOptions.DuplicatePolicy is switchable). Revisit when mod patching lands."
    },
    {
      "id": "automerge",
      "date": "2026-09-06",
      "state": "decided",
      "title": "Auto-merge green PRs",
      "note": "PRs are merged as soon as CI is green; no manual merge step."
    },
    {
      "id": "threadstatic",
      "date": "2026-09-06",
      "state": "decided",
      "title": "RimWorld's static Rand and Scribe stay static, but per thread",
      "note": "Ported call sites (Rand.Value, Scribe_Values.Look) keep their shape; state is [ThreadStatic] so tests and worker threads never share a sequence or a save session. Explicit RandomStream instances are the preferred form for new systems."
    },
    {
      "id": "timescale",
      "date": "2026-09-07",
      "state": "decided",
      "title": "Two clocks; time never causes progression",
      "note": "The ported 60-tick clock stays authoritative. An abstracted clock above it exists only to skip, and may only do what the real clock would have done — every process it advances needs a sampled equivalent that agrees with ticking. A century of abstract time with no people, food or research advances nothing. Spec section 11.1."
    },
    {
      "id": "attention",
      "date": "2026-09-07",
      "state": "decided",
      "title": "Civilization-wide sight, settlement-deep touch",
      "note": "Attention follows RimWorld's world-map/colony-map split, which World/ and Map/ already mirror. Global sight and indirect verbs at civilization scope; any citizen openable at settlement scope. The missing seam is the settlement interior. Spec section 11.2."
    },
    {
      "id": "lod",
      "date": "2026-09-07",
      "state": "decided",
      "title": "Citizens tier by significance, and the tier is the historical record",
      "note": "Full / Interval / Statistical, promoted by attention, role, chronicle mention or relation — never by distance or elapsed time; identity survives demotion. Fidelity is written at the tier lived and never upgraded, so the deep past is thin because it was thin. One mechanism pays for both performance and the fiction. Spec section 11.3-11.4."
    },
    {
      "id": "gamestart",
      "date": "2026-09-07",
      "state": "decided",
      "title": "A game opens region first, then site, Manor Lords style",
      "note": "The world map is browsed by region; inside the chosen region, markers show water, soil, timber, stone, clay, flint, ore, salt, game, fords and high ground, and the player places the settlement centre. Deposits are derived from existing terrain rather than sprinkled. Founding band is 20-40 people in several households. Spec section 5b."
    },
    {
      "id": "solostart",
      "date": "2026-09-07",
      "state": "decided",
      "title": "One civilization at world generation; rivals emerge later",
      "note": "World gen places the player's faction alone. Other civilizations emerge from the simulation over time rather than existing at time zero. The cost — factions, trade and diplomacy idle through the opening — is accepted for the truer sticks-and-stones arc. Spec section 5b.4."
    },
    {
      "id": "horizon",
      "date": "2026-09-08",
      "state": "decided",
      "title": "No fixed horizon — the director paces time",
      "note": "Decided: years are not the currency, attention-worthy moments are. A civilization runs on the abstracted clock by default and drops into ticked time when the storyteller judges a moment worth watching — RimWorld's own TimeSlower inverted, and TimeSlower is already ported. A century in which nothing happened costs nothing to pass, which is what makes the endless sticks-to-exotic arc playable at all: at the real clock, 200 ticked years would be 222 hours at maximum speed. The tech tree is therefore priced against moments per era rather than years, and the 'dry at day 3,074' measurement is a function of a unit that no longer applies. Spec section 11.1."
    },
    {
      "id": "perf",
      "date": "2026-09-07",
      "state": "open",
      "title": "The population ceiling is health-tracker cost",
      "note": "Measured: 14 milliseconds per pawn-day at N<=500 (an earlier note said microseconds, off by a thousand), 15x speed holds to roughly 2,500-5,000 healthy pawns, and HealthTick is 75.4% of all per-pawn tick cost. Two fixes landed: making the hot capacity path non-allocating cut garbage 31x (4,232 MB to 138 MB per 1,000 pawn-days, 33 Gen0 collections to 1), and caching the per-tick death check behind the existing dirty flag took a further 7-12% (measured A/B, same box same minute). The remaining cost is spread rather than concentrated, so the next real lever for population scale is the citizen tiering in spec section 11.3, not another micro-optimization. Sick pawns remain far worse: 3 untended injuries measured 24.8x. See docs/perf/baseline.md."
    },
    {
      "id": "tiering",
      "date": "2026-09-08",
      "state": "decided",
      "title": "Citizen tiering is built next, before the god layer",
      "note": "It is the most invasive change remaining — it touches every tracker and every system that assumes a pawn is fully simulated — so the god layer should be written against tiered citizens rather than retrofitted to them. RimWorld already proves the Statistical tier works: its world pawns keep identity, relationships and ageing while needs, jobs and health stop ticking, and AgeTickMothballed is already ported. The tiers and the abstracted clock are one mechanism: dropping into ticked time is promoting the attended settlement to Full. Spec section 11.3."
    },
    {
      "id": "coal",
      "date": "2026-09-08",
      "state": "decided",
      "title": "Coal is its own deposit, and tradeable",
      "note": "Coal decided where industry went, so it gets its own category rather than hiding inside Ore: a site chosen in the neolithic for water and game may or may not turn out to be an industrial one, and the player will not know for centuries. Geography shapes rather than dictates — caravans can supply a settlement that lacks coal, at a cost, which is what actually happened and gives the ported trade system real work in the industrial era. Spec section 5b."
    }
  ],
  "systems": [
    {
      "id": "defs",
      "n": 1,
      "rimworld": "Data & Modding Architecture",
      "translation": "Unchanged. Defs, Comps and patching are engine-neutral; every later module is data-driven through this layer.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Defs · Defs/Patches · Content (ModContentPack, ModLoadOrder)",
      "status": "ported",
      "tests": 103,
      "items": [
        {
          "id": "defs.xml",
          "label": "XML Defs loading: <Defs> root, Def type resolved from element name",
          "done": true
        },
        {
          "id": "defs.mapping",
          "label": "Reflection field mapping: primitives, enums, nested objects, lists, dictionaries",
          "done": true
        },
        {
          "id": "defs.inherit",
          "label": "Name / ParentName / Abstract inheritance with Inherit=\"False\" and list append",
          "done": true
        },
        {
          "id": "defs.class",
          "label": "Class=\"…\" polymorphism and Type-valued fields (thingClass, compClass)",
          "done": true
        },
        {
          "id": "defs.crossref",
          "label": "Deferred cross-references across files (fields, lists, dictionary values)",
          "done": true
        },
        {
          "id": "defs.database",
          "label": "DefDatabase: typed registries incl. subclasses, duplicate policy, RimWorld-style static facade",
          "done": true
        },
        {
          "id": "defs.defof",
          "label": "DefOf static binding with DefAlias",
          "done": true
        },
        {
          "id": "defs.lifecycle",
          "label": "Lifecycle order: PostLoad → cross-refs → ResolveReferences → ConfigErrors",
          "done": true
        },
        {
          "id": "defs.comps",
          "label": "CompProperties (data half of the Comp pattern) as a Class= list on ThingDef",
          "done": true
        },
        {
          "id": "defs.stats",
          "label": "StatDef / StatModifier with the element-name-as-stat idiom",
          "done": true
        },
        {
          "id": "defs.custom",
          "label": "IXmlCustomLoad hook, LoadAlias, Unsaved, IsNull",
          "done": true
        },
        {
          "id": "defs.parse",
          "label": "Culture-invariant ParseHelper incl. IntRange, FloatRange, CurvePoint/SimpleCurve",
          "done": true
        },
        {
          "id": "defs.thingcomp",
          "label": "ThingComp runtime half (ThingWithComps.InitializeComps)",
          "done": true
        },
        {
          "id": "defs.patches",
          "label": "Content packs and XPath patching: PatchOperation Add/Insert/Remove/Replace, the three attribute operations, SetName, and Sequence/Conditional/FindMod for control flow. Patches run against the combined document before inheritance resolves, so one edit to an abstract parent reaches every def derived from it; an operation matching nothing is an error unless it declares success Always; a def a patch adds is credited to the patching pack",
          "done": true
        },
        {
          "id": "defs.loadorder",
          "label": "ModContentPack (About/About.xml, Defs/, Patches/) with Core as a pack like any other, and ModLoadOrder: a stable topological sort over modDependencies/loadAfter/loadBefore with declared order as the tie-break, Core always first. A missing dependency and a constraint cycle are reported and survived rather than thrown",
          "done": true
        },
        {
          "id": "defs.wiringaudit",
          "label": "A wiring audit, because the defect this project keeps finding is a module that is complete, tested and called by nothing — and a suite that tests each module in isolation is structurally blind to it. Six checks over reflection, a token index of src/ and the loaded content graph: an interface with no implementation the game can hold, a Notify_ hook with no call site, a Def field naming a placeholder or abstract worker, a field code reads that no Def sets, a field content sets that no code reads, and state read but never written. Evidence is weighed by which answer raises a seam: strict where a use creates an entry, generous where a use silences one — applying strict scoping to both produced 52 false reports",
          "done": true
        },
        {
          "id": "defs.wiringaudit.baseline",
          "label": "Ships with a reviewed baseline of 131 dormant seams, each carrying a hand-written reason, so the audit is green today and loud the moment a new seam goes dark. Stale lines fail too, so the file can only shrink — wiring WardenDeliverFood in the same batch already retired one entry. Proved non-vacuous three ways: every check run against a fixture both dangling and wired, a planted hook on the real tree, and a guard asserting the audit is holding the whole core rather than an empty walk",
          "done": true
        },
        {
          "id": "defs.wiringaudit.findings",
          "label": "129 dormant seams found that nobody knew about. The ones that matter: NeedDef.fallPerDay is zero for all eight needs so the generic decay path can never fire; StoryWatcher_Adaptation.Notify_ColonistDowned has no caller — the exact twin of the Notify_ColonistDied bug the audit was written after; IQuestRewardSink has no implementation, so quests compute rewards into nothing; ThingDef.mineableThing and mineableYield are read by nothing while JobDriver_Mine's own doc claims it reads them, so mining destroys rock and drops nothing; trait shaping never happens (forcedTraits, disallowedTraits, requiredWorkTags all unset in content); and difficulty is nearly inert — cropYieldFactor, mineYieldFactor, colonistMoodOffset and questRewardValueFactor are all set in content and read by nothing",
          "done": false
        }
      ]
    },
    {
      "id": "worldgen",
      "n": 2,
      "rimworld": "World Generation",
      "translation": "Implemented: planet generation at civilization scale — biomes, rivers and roads unchanged; the player's civilization alone at world gen, many rival civilizations seeded over time instead by World.EmergenceManager, population-scaled and era-seeded off the world's current era.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/World · Noise · Factions (minimal) · World.EmergenceManager",
      "status": "ported",
      "tests": 39,
      "items": [
        {
          "id": "worldgen.tiles",
          "label": "Icosphere tile grid (10·4ⁿ+2 tiles, 5/6 neighbours, lat/long) with coverage → subdivision",
          "done": true
        },
        {
          "id": "worldgen.noise",
          "label": "Seeded Perlin/ridged-multifractal noise → elevation (~32% land), hilliness, temperature by latitude and elevation, rainfall, swampiness",
          "done": true
        },
        {
          "id": "worldgen.biomes",
          "label": "14 biome workers scoring tiles (ocean/lake by water-body size; tundra…rainforest; deserts)",
          "done": true
        },
        {
          "id": "worldgen.rivers",
          "label": "Rivers flow downhill by flow thresholds; roads path between settlements over hilliness/biome cost",
          "done": true
        },
        {
          "id": "worldgen.factions",
          "label": "Minimal FactionDef/Faction + settlement placement (counts, min distance, biome weight); world save regenerates the grid from its seed",
          "done": true
        },
        {
          "id": "worldgen.relations",
          "label": "Faction relations and tech levels seeded at world generation",
          "done": true
        },
        {
          "id": "worldgen.civscale",
          "label": "Translation: many rival civilizations at planet scale — population scaling and era seeding — World.EmergenceManager, MTB-paced off OverallPopulation and gated by Research.ResearchManager.CurrentEra so nothing emerges more advanced than the world's own most advanced known civilization",
          "done": true
        }
      ]
    },
    {
      "id": "mapgen",
      "n": 3,
      "rimworld": "Local Map Generation",
      "translation": "Settlement-site maps, many per civilization, generated on demand from the world tile via MapGen.MapGenerator.GenerateMapFor, rather than RimWorld's single fixed map. Rocky outcrops, ore veins and river channels are not sprinkled independently of the world map: their presence and rough scale are read directly from the tile's own Ore/Stone/river data, so a tile the world map promised ore or a river on always produces a map with ore or a river crossing it. Ruins (GenStep_Ruins) are a direct, hand-written stand-in for RimWorld's RuleDef/SymbolResolver ruin grammar rather than a port of that grammar itself: one ruin shape (a gap-and-rubble wall rectangle, sometimes roofed, sometimes looted, walls weathered off full hit points), not a composable rule library. GenStep_ScatterShrines (a single guarded unique-reward structure) and corpses inside a ruin are not ported at all — no Corpse Thing/system exists yet in this port, and a flavor-only stand-in was judged to misrepresent rather than simplify a dead pawn's identity; see GenStep_Ruins's own doc comment.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/MapGen",
      "status": "ported",
      "tests": 27,
      "items": [
        {
          "id": "mapgen.terrain",
          "label": "Terrain layer from tile biome, fertility/rainfall noise and swampiness (soil, rich soil, sand, gravel, marsh), plus a carved river channel where the tile carries one",
          "done": true
        },
        {
          "id": "mapgen.elevation",
          "label": "Elevation/fertility noise → rocky outcrops and mountains as natural edifices, scaled by tile hilliness and the Stone deposit magnitude (per-map quantile calibration, mirroring WorldGenStep_Terrain's land-fraction technique)",
          "done": true
        },
        {
          "id": "mapgen.caves",
          "label": "Cave systems (directional random walk, not cellular automata) carved through mountain, with mineable ore veins guaranteed proportional to the tile's Ore deposit magnitude and a yield table (mineableThing/mineableYield)",
          "done": true
        },
        {
          "id": "mapgen.roofs",
          "label": "Roofs (thick/thin) over solid mountain and enclosed cave passages via the RoofGrid, graded by local rock density rather than a border flood fill (a mined tunnel stays roofed along its length)",
          "done": true
        },
        {
          "id": "mapgen.scatter",
          "label": "Scatterers: loose rock chunks and wild plant growth, densities read from the tile's Stone/Timber deposit magnitudes and biome plant density",
          "done": true
        },
        {
          "id": "mapgen.seam",
          "label": "MapGenerator.GenerateMapFor(worldTile): a settlement's interior generated from the world tile it sits on — the §11.2 seam between the World and Map layers",
          "done": true
        },
        {
          "id": "mapgen.ruins",
          "label": "GenStep_Ruins: weathered wall rectangles with gaps and rubble scattered onto the map, some roofed, some carrying loot, material and roofing drawn from content; deterministic per seed, density scaling with map area, and structurally guaranteed (plus region-graph tested) never to wall a pawn into an unreachable pocket. GenStep_ScatterShrines and ruin corpses are not ported — see the module's own doc comment",
          "done": true
        },
        {
          "id": "mapgen.roads",
          "label": "World-tile roads (RoadDef/tile.Roads) carried onto the local map as a street of the road's own local terrain (RoadDef.localTerrain), entering and leaving on the sides that face the neighbouring tiles the road actually connects — direction read from WorldGrid tile positions via GenStep_Roads",
          "done": true
        },
        {
          "id": "mapgen.entry-trigger",
          "label": "Entering a settlement at settlement scope triggers GenerateMapFor and persists the result on the WorldObject — Settlement.EnterMap, generated once on first entry, sized by TotalPopulation, cached and Scribe round-tripped",
          "done": true
        }
      ]
    },
    {
      "id": "mapcore",
      "n": 4,
      "rimworld": "Map Core & Thing Runtime",
      "translation": "Unchanged. The Thing/Comp runtime and map grids every later system stands on; settlement maps are generated onto it.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Map · Things",
      "status": "ported",
      "tests": 56,
      "items": [
        {
          "id": "mapcore.geometry",
          "label": "Geometry: IntVec3/IntVec2, Rot4, CellRect, GenAdj, GenRadial, CellIndices",
          "done": true
        },
        {
          "id": "mapcore.things",
          "label": "Thing / ThingWithComps / ThingComp / ThingMaker: the map-aware Thing runtime and the Comp pattern's runtime half",
          "done": true
        },
        {
          "id": "mapcore.pawnthing",
          "label": "Pawn is a Thing: identity, map presence, hit points and ticking move onto ThingWithComps",
          "done": true
        },
        {
          "id": "mapcore.grids",
          "label": "Terrain, roof, thing, edifice and path grids with path-cost recalculation",
          "done": true
        },
        {
          "id": "mapcore.lister",
          "label": "ListerThings and MapPawns: per-def, per-group and per-map bookkeeping",
          "done": true
        },
        {
          "id": "mapcore.scribe",
          "label": "Map save/load: run-length terrain and roof grids plus a polymorphic spawned-Thing list",
          "done": true
        },
        {
          "id": "mapcore.regions",
          "label": "Region / RegionLink graph over the map's passability (Region, RegionLink, RegionGrid, RegionMaker, RegionAndRoomUpdater), cardinal flood fill with doors as their own Portal regions, torn down and re-flooded only for the region(s) a passability change actually touches — AI's Reachability is the consumer",
          "done": true
        },
        {
          "id": "mapcore.mining",
          "label": "Mining yields what the rock holds — ThingDef.mineableThing/mineableYield were set by content and read by nothing: JobDriver_Mine destroyed the edifice and spawned air. Mineable/MineableUtility now drop the chunk or ore, GenStep_RocksAndMountains places more than one rock type, and DifficultyDef.mineYieldFactor finally has a yield to scale.",
          "done": true
        }
      ]
    },
    {
      "id": "pawngen",
      "n": 5,
      "rimworld": "Pawn Generation",
      "translation": "Proposed: citizen generation. Birth and lineage drive most generation instead of 'wanderer joins'; backstories become life histories written by the sim.",
      "translationState": "proposed",
      "module": "src/SimWorld.Core/Pawns · Pawns/Generation · Pawns/Genes",
      "status": "ported",
      "tests": 65,
      "items": [
        {
          "id": "pawngen.backstory",
          "label": "Backstory pairs (childhood/adulthood): skill gains, disabled work tags, forced traits",
          "done": true
        },
        {
          "id": "pawngen.traits",
          "label": "Trait rolls by commonality with degrees, conflicts and required work tags",
          "done": true
        },
        {
          "id": "pawngen.skills",
          "label": "Skill levels from age-scaled curves plus backstory and trait gains; passion rolls",
          "done": true
        },
        {
          "id": "pawngen.names",
          "label": "NameTriple/NameSingle from gendered name banks, unique across a population",
          "done": true
        },
        {
          "id": "pawngen.ages",
          "label": "Life stages and age tracker: biological/chronological ticks, body size, health scale and hunger factors per stage",
          "done": true
        },
        {
          "id": "pawngen.appearance",
          "label": "Appearance: melanin and body type only — hair and palette land with the renderer",
          "done": true
        },
        {
          "id": "pawngen.lineage",
          "label": "Translation: newborn generation and life events — birth and lineage as the main source of citizens is still open",
          "done": true
        },
        {
          "id": "pawngen.gear",
          "label": "Weapon half: PawnKindDef.weaponTags/weaponMoneyRange + PawnWeaponGenerator, matched against ThingDef.weaponTags/techLevel/MarketValue and capped at the generated pawn's own Faction.def.techLevel; wired into PawnGenerator's pipeline and carried on the new Pawn_EquipmentTracker (Pawn.equipment), Scribe round-trip included. Apparel half now built too — see pawngen.apparel.",
          "done": true
        },
        {
          "id": "pawngen.apparel",
          "label": "Apparel half of gear: ThingDef.apparel/ApparelProperties (body part groups, ApparelLayerDef, tags) plus Pawn_ApparelTracker — wearing an item registers it as a real Combat.IArmorSource through the pre-existing PawnArmor hook, no Combat code touched. PawnApparelGenerator consumes PawnKindDef.apparelTags/apparelMoneyRange the way PawnWeaponGenerator consumes weaponTags, wired into PawnGenerator after the lifespan roll so it never perturbs an RNG-sensitive step any other test already pins. Six ThingDefs ship (Apparel_Basic.xml) with real ArmorRating_*/Insulation stats and CompQuality. Tests: tests/SimWorld.Core.Tests/Things/ApparelTests.cs (filed with CompQuality/apparel content rather than under Pawns, per this pass's test-directory ownership).",
          "done": true
        },
        {
          "id": "pawngen.genes",
          "label": "Genes / xenotypes with inheritance (Biotech): GeneDef (statOffsets/statFactors/capMods/disabledWorkTags/biostatCpx/biostatMet/lifespanBonusYears/exclusionTags) + XenotypeDef (a named germline), Pawn_GeneTracker (endogenes/xenogenes, exclusionTags conflict resolution) reusing StatWorker/PawnCapacityUtility/CombinedDisabledWorkTags/HungerRate rather than a parallel pipeline; PawnGenerationRequest.Xenotype applied by PawnGenerator strictly downstream of every existing roll (zero RNG cost when unset); FamilyManager birth-path inheritance (GeneInheritanceUtility) — shared endogenes breed true, a single-parent endogene is a 50% coin flip, xenogenes never inherit. RimWorld's exact inheritance-selection and metabolism-to-hunger curves were not sourced in this sandbox — see GeneTuning's own doc; pinned by property tests instead of literals. Content: 14 GeneDefs, 4 XenotypeDefs (Baseliner + 3 non-baseline).",
          "done": true
        },
        {
          "id": "pawngen.traitshaping",
          "label": "Backstories, kinds and germlines shape traits. Eight seams in one cluster — BackstoryDef and PawnKindDef's forcedTraits/disallowedTraits, TraitDef.requiredWorkTags, and TraitDegreeData's disabledWorkTags/disallowedMentalBreaks/theOnlyAllowedMentalBreaks — were all read by PawnGenerator and set by no shipped content, so every pawn in every game rolled from the same unfiltered table. Content sets all eight, and three code defects surfaced in the wiring: TryForceTrait skipped the capability gate the random path enforces (a backstory could force bloodlust onto a pawn barred from violence); backstories ignored their own requiredWorkTags, so a pacifist could be a tribal hunter; and genes were applied after the two filters that need to read GeneDef.disabledWorkTags, with Pawn_GeneTracker never raising the disabled-work-tags notification. Contradictions in the forced/disallowed pair are a config error now (BackstoryTraitValidation) rather than a generator that silently skips.",
          "done": true
        }
      ]
    },
    {
      "id": "simcore",
      "n": 6,
      "rimworld": "Simulation Core / Game Loop",
      "translation": "Unchanged. Tick engine, rare/long tick buckets, deterministic RNG streams, Scribe-style save/load. Bucket sizing scales with population.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Sim",
      "status": "ported",
      "tests": 112,
      "items": [
        {
          "id": "simcore.tick",
          "label": "TickManager: 60 ticks/s clock, TimeSpeed multipliers (1/3/6/15), pause/toggle, danger-forced normal speed",
          "done": true
        },
        {
          "id": "simcore.buckets",
          "label": "TickList buckets: normal / rare (250) / long (2000) with deferred register/deregister",
          "done": true
        },
        {
          "id": "simcore.order",
          "label": "Tick order: pre-tickers (world, map) → normal → rare → long → post-tickers; per-frame tick cap drops backlog",
          "done": true
        },
        {
          "id": "simcore.rng",
          "label": "MurmurHash-based RandomStream per system + thread-safe static Rand facade (Range/Chance/Gaussian/MTB/PushState)",
          "done": true
        },
        {
          "id": "simcore.calendar",
          "label": "GenTicks / GenDate: 60,000-tick days, quadrums, years, longitude-local time, seasons by latitude",
          "done": true
        },
        {
          "id": "simcore.scribe",
          "label": "Scribe: IExposable save/load in three passes (LoadingVars → ResolvingCrossRefs → PostLoadInit)",
          "done": true
        },
        {
          "id": "simcore.scribe-modes",
          "label": "Scribe_Values / Defs / Deep (polymorphic Class=) / References (forward + cyclic) / Collections (list, set, dictionary)",
          "done": true
        },
        {
          "id": "simcore.game",
          "label": "Game root (Sim/Game.cs): owns World, the live Maps (a settlement's entered interior), and every manager that was previously only reached through Find (research, storyteller + CivilizationTarget, factions, letters, quests, scenario, family, social, god). Find.CurrentGame makes Find.* resolve through it when one exists and fall back to the pre-Game thread-static behaviour unchanged when it doesn't. Game.NewGame runs world generation, founds the starting settlement (SettlementFounder) and Scenario.PostGameStart against the actual founding population, then wires the defined pre/post tick order. Whole-game Scribe round trip (world + settlement + population + elapsed time), reloaded and re-ticked.",
          "done": true
        },
        {
          "id": "simcore.autosave",
          "label": "Autosave cadence and hook (Game.AutosaveIntervalTicks / Game.AutosaveDue): fires on a cadence wired into the post-tick order; writing the save file is left to the host, per the module's own scope",
          "done": true
        },
        {
          "id": "simcore.scribe-stream",
          "label": "Streaming XmlWriter save path: ScribeSaver drives a System.Xml.XmlWriter rather than building an XDocument, which is what RimWorld's own saver always did. Scribe.SaveToStream/SaveToFile write straight to a sink, so content leaves as the writer's buffer fills and peak memory stops tracking save size; Scribe.SaveToXDocument/SaveToString keep their exact shape and text by pointing the same writer at an XDocument. Streaming adds one rule the port already obeyed — an attribute must precede its node's child content — and WriteAttribute now throws rather than silently dropping one. Loading still parses whole: the three-pass load re-reads nodes after cross-refs resolve, which a forward-only reader cannot serve",
          "done": true
        },
        {
          "id": "simcore.intrange",
          "label": "Rand.Range(IntRange) was max-exclusive while RandomStream.Range(IntRange) was max-inclusive — same name, same parameter type, different distribution, and RimWorld's IntRange.RandomInRange is inclusive, so the static one was the odd one out. Both overloads now delegate rather than re-derive, so they cannot drift again. A degenerate range also used to consume no draw where RimWorld's does, so the stream now matches",
          "done": true
        },
        {
          "id": "simcore.hashphasing",
          "label": "Pawn.HashOffsetTicks multiplied the thing id by 3, so gcd(3,30) put every pawn on a third of the available phases at 3x the intended peak for every hash-interval system. Now hashed. Measured at 500 pawns on a real map: peak tick fell from 41% above the mean to 10% above (1.80ms to 1.39ms), with the mean unmoved as the control — phasing moves work between ticks and never removes any, so a moved mean would have meant the measurement was wrong",
          "done": true
        },
        {
          "id": "simcore.hashphasing.turret",
          "label": "CompTurretGun and Fire open-coded the same thingIDNumber * 3 and clustered identically — recorded as a follow-up rather than fixed in place, so the fix would be one shared helper and not a third copy. Sim.HashInterval is that helper, and the clustering was real: turret scans run every 15 ticks so every turret landed on one of 5 reachable phases instead of 15, and fire's complex calcs every 150 so every fire landed on one of 50 instead of 150 — three deep in both cases, and a burning quarter is precisely what that interval exists to spread. Pawn.HashOffsetTicks delegates to the same helper and keeps the bench's legacy-offset hatch. The test states the old behaviour at both intervals, so the coverage assertions are demonstrably sensitive to what they rule out rather than passing vacuously.",
          "done": true
        }
      ]
    },
    {
      "id": "needs",
      "n": 7,
      "rimworld": "Needs & Mood",
      "translation": "Unchanged per citizen. Adds aggregate 'public mood' rollups so the god view can read a city without opening every person.",
      "translationState": "proposed",
      "module": "src/SimWorld.Core/Needs · Thoughts · MindState · Pawns",
      "status": "ported",
      "tests": 84,
      "items": [
        {
          "id": "needs.pawn",
          "label": "Pawn skeleton + RaceProperties: needs/story/mind trackers, hash-interval ticking, Scribe round-trip",
          "done": true
        },
        {
          "id": "needs.defs",
          "label": "NeedDef set: Food, Rest, Joy, Mood, Beauty, Comfort, Outdoors, RoomSize (intelligence-gated)",
          "done": true
        },
        {
          "id": "needs.decay",
          "label": "Decay rules: food 1.6/day slowing when hungry, rest fall/gain with fatigue factors, joy tolerance, seekers",
          "done": true
        },
        {
          "id": "needs.thoughts",
          "label": "Thoughts: timed memories (stack limit, renew-oldest) + situational workers, trait nullifying/required",
          "done": true
        },
        {
          "id": "needs.mood",
          "label": "Mood = 50% + grouped thought total with RimWorld's geometric stacking rule; seeker rates",
          "done": true
        },
        {
          "id": "needs.breaks",
          "label": "MentalBreaker bands (0.35/0.20/0.05), 2000-tick grace, MTB rolls, weighted trait-filtered break table, state lifecycle + Catharsis",
          "done": true
        },
        {
          "id": "needs.traits",
          "label": "TraitDef/Trait/TraitSet with degrees, conflicts and exclusion tags",
          "done": true
        },
        {
          "id": "needs.content",
          "label": "Core XML: Human/Husky races, needs, joy kinds, traits, thoughts, mental states and breaks",
          "done": true
        },
        {
          "id": "needs.social",
          "label": "Social/opinion thoughts and social fights: Thought_Memory carries a nullable otherPawn and feeds opinion (ThoughtDef.IsSocial, ThoughtStage.baseOpinionOffset) as well as mood; the Social module's interaction workers gain those memories, so chitchat/deep talk/insult/slight move opinion. A social fight is a real MentalStateDef (MentalState_SocialFighting) that trades blows through Combat's existing Verb_MeleeAttack/MeleeVerbUtility path rather than a parallel system, and ends at the Def's duration/MTB recovery or the instant either side goes down. Situational relation-driven thoughts (HasFriend/HasRival, via the generic ThoughtWorker_HasDirectRelation) stand in for RimWorld's spatial \"friend nearby\"/\"rival present\" thoughts, since no Map/room concept reaches this seam yet.",
          "done": true
        },
        {
          "id": "needs.rollup",
          "label": "Translation: population mood rollups for the god view. Satisfied by God/GodRollup.cs's MeanMood — a population-weighted mean of Need_Mood.CurLevelPercentage, tier-aware (a Statistical citizen's need is cohort-sampled the same as any other, real or folded from a Settlement's bare StatisticalPopulation count), cached with a recompute cadence and an explicit dirty flag, fed by a real Settlement (or several, for a civilization) as of god.settlement-population. See docs/spec/simworld-spec.md §10's Aggregation bullet.",
          "done": true
        },
        {
          "id": "needs.filth",
          "label": "Filth: Verse.Filth and FilthMaker, with cleaning bounded by the home area when one is set and any enclosed roofed room when none is — the home area shipped dormant, so bounding on it alone would have made every fire of the giver impossible. Feeds room cleanliness into surgery and a filthy-room thought",
          "done": true
        },
        {
          "id": "needs.beauty",
          "label": "The Beauty need samples the map for real. IEnvironmentSampler had no implementation in src at all — only a test fake — so Beauty sat at its base level forever for every citizen in every game. MapEnvironmentSampler answers from the citizen's Room when enclosed and a 6.9 radius otherwise, gated to Full tier so the coarse-tick path costs nothing. Shipped content had no Beauty stat at all, which would have averaged zero everywhere and left the system dormant on arrival, so filth and a buildable sculpture now carry one. Measured at 0.35s per in-game day at the Full-tier budget, most of it not new spending",
          "done": true
        },
        {
          "id": "needs.filththought",
          "label": "ThoughtWorker_FilthyRoom retired as its own doc instructed: it read room cleanliness directly only because Beauty was unavailable, and keeping it would have charged filth to mood twice. Its tests now assert the same properties through the beauty route",
          "done": true
        },
        {
          "id": "needs.decaywiring",
          "label": "Needs that actually move. NeedDef.fallPerDay was zero for all eight needs and no needClass reached the generic decay it fed, so the path was dead twice over — and RimWorld has no such path at all (NeedInterval is abstract there, and fallPerDay's one reader is Need_Chemical, unbuilt here). NeedInterval/NeedIntervalBulk are abstract now, so a need cannot inherit a decay that does nothing. Need_Outdoors ported 1:1 (roof overhead plus open-to-weather, RimWorld's six per-day deltas and the 0.2 thin-roof floor); Comfort and RoomSize still hold still and now say so in their own Def. Two arithmetic defects surfaced: Need_Joy.NeedIntervalBulk took elapsed/150 as an integer and dropped the remainder, losing 50 ticks of decay every 2,000-tick coarse tick, and its category multipliers were nobody's numbers.",
          "done": true
        },
        {
          "id": "needs.memoryageing",
          "label": "A memory an unwatched citizen held aged thirteen times too slowly: Need_Seeker.NeedIntervalBulk ran one thought interval (150 ticks) however long the elapsed span was, and an Interval-tier citizen's mood advances once per 2,000-tick coarse tick. The mood LEVEL always agreed between the two paths — which is why the existing agreement test passed — but the memories underneath it expired at a thirteenth of real speed, so most of the civilization stayed anchored to events a fortnight after a watched citizen had forgotten them. ThoughtIntervalBulk(elapsedTicks) is one ageing path for both tiers, O(#memories) either way.",
          "done": true
        }
      ]
    },
    {
      "id": "health",
      "n": 8,
      "rimworld": "Health System",
      "translation": "Unchanged.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Health · Data/Core/Defs (bodies, hediffs, damages)",
      "status": "ported",
      "tests": 119,
      "items": [
        {
          "id": "health.body",
          "label": "BodyDef part tree: tags, groups, height/depth, coverage → absolute coverage for hit selection (Human + quadruped)",
          "done": true
        },
        {
          "id": "health.hediffs",
          "label": "Hediffs: severity-staged conditions with comps (immunizable, tend duration, severity/day, disappears, gets-permanent, infecter); injuries, missing parts, added parts",
          "done": true
        },
        {
          "id": "health.capacities",
          "label": "11 capacities from part efficiency (tag averages, limb products, cap mods); downed = pain shock / unconscious / no legs, dead = lethal capacity, core part, lethal severity or 150 total damage",
          "done": true
        },
        {
          "id": "health.damage",
          "label": "DamageDef → worker → injury on a coverage-weighted part; solid parts crack, skin bruises; part destruction removes the subtree",
          "done": true
        },
        {
          "id": "health.pain",
          "label": "Pain aggregate (Σ offsets × Π factors) → consciousness penalty, pain shock, Pain/Sick thoughts",
          "done": true
        },
        {
          "id": "health.healing",
          "label": "Bleeding → blood loss (lethal at 1), natural healing 8/day (+4 lying, +22×quality tended), scars, infection rolls, tending priority",
          "done": true
        },
        {
          "id": "health.immunity",
          "label": "Immunity vs severity race per disease (flu survivable, plague/infection lethal untended); malnutrition from starvation; hediff hunger/rest factors",
          "done": true
        },
        {
          "id": "health.content",
          "label": "Core XML: 23 part tags, 13 groups, 38 part defs, 2 bodies, 11 capacities, 16 hediffs, 6 damages, health thoughts",
          "done": true
        },
        {
          "id": "health.addictions",
          "label": "Drug ThingDefs (Beer/Alcohol, Flake/Yayo) carry a real CompDrug: tolerance builds per dose and decays per day (HediffCompProperties_SeverityPerDay, already existing), addiction rolls once tolerance is at ChemicalDef.minToleranceToAddict, and a further dose once addicted relieves severity instead of stacking a second addiction. Withdrawal reaches Need_Mood purely through content pointed at the pre-existing ThoughtWorker_Hediff hook (Thoughts_Drugs.xml) — no new Needs/Thoughts code. A dose's joy uses the existing Chemical JoyKindDef. Deliberately out of scope, and not RimWorld-sourced: overdose, and any chance of an addiction curing itself outright (see CompDrug's own remarks). Tests: tests/SimWorld.Core.Tests/Health/DrugTests.cs.",
          "done": true
        },
        {
          "id": "health.surgery",
          "label": "Surgery bills with skill-rolled outcomes: RecipeDef.isSurgery + a Class=-selected Recipe_Surgery worker (amputate, excise a hediff, install a part), queued as a Bill_Medical on the patient’s own bill stack. Success is the surgeon’s Medicine skill times the recipe’s difficulty; a failure injures the patient and may kill them, never silently nothing. Two surgeries ship as content; installing a prosthetic now ships as content too (InstallSimpleProstheticLeg) — see health.prosthetic-items",
          "done": true
        },
        {
          "id": "health.prosthetic-items",
          "label": "Prosthetics as real items: SimpleProstheticLeg ThingDef (Item, hasQuality + CompProperties_Quality, MarketValue set) ships tradeable — no crafting recipe, since no fabrication workbench exists yet (the same \"craftable or tradeable\" allowance this item took). InstallSimpleProstheticLeg ships as content, naming the item as a RecipeDef.ingredients entry; SurgeryUtility.PerformNextSurgery gained an optional ingredientsOnHand parameter so a bill naming ingredients only fires once a matching Thing is supplied and consumed (decremented/destroyed) — every ingredient-less surgery (amputate, excise) is unaffected, matching RecipeDef.ConfigErrors' existing surgery/non-surgery split. Tests: tests/SimWorld.Core.Tests/Health/SurgeryTests.cs.",
          "done": true
        },
        {
          "id": "health.tending-work",
          "label": "Doctor work givers close the 'no one tends an injury' gap (docs/WORK-REGISTER.md's work-economy item): WorkGiver_Tend backs both DoctorTendEmergency and DoctorTend (one class, split by the WorkGiverDef's own emergency flag against TendUtility.NeedsEmergencyTend — bleeding or life-threatening), JobDriver_TendPatient tends at a quality read from StatDefOf.MedicalTendQuality (work.stats) capped at TendUtility.MaxQualityNoMedicine (no medicine ThingDef ships yet). WorkGiver_RescueDowned/JobDriver_TakeToBed carry a downed patient to AI.RestUtility.FindBedFor's nearest reachable bed, or produce no job at all when the map has none — tended and fed in place rather than invented a ground fallback. WorkGiver_FeedPatient (DoctorFeedHumanlikes) reuses WardenFeed's own FeedPatient JobDef/JobDriver unchanged for a downed, hungry non-prisoner, explicitly excluding prisoners (WardenFeed's own patient) rather than racing it for the same reservation — the same reasoning leaves WardenDeliverFood unwired, unchanged from before this item. Eligibility (colonist of the doctor's own faction, or a prisoner it holds) is shared by Tend and Rescue via AI.DoctorUtility. Deliberately out of scope: self-tend (RimWorld's own reduced-quality solo-colonist mechanic — a pawn is never its own patient here, same as RimWorld's WorkGiver_Tend). Code: src/SimWorld.Core/AI/WorkGiver_Tend.cs, JobDriver_TendPatient.cs, WorkGiver_RescueDowned.cs, JobDriver_TakeToBed.cs, WorkGiver_FeedPatient.cs, DoctorUtility.cs; TendUtility.NeedsEmergencyTend in src/SimWorld.Core/Health/TendUtility.cs. Tests: tests/SimWorld.Core.Tests/Health/DoctorAITests.cs.",
          "done": true
        },
        {
          "id": "health.fire",
          "label": "Fire: Verse.Fire ported as an AttachableThing that grows, damages, spreads and is beaten out, with BaseFlammability through the real stat pipeline. ThingDef carried no flammability field at all and no content was flammable, so a stone or steel wall now survives what a wooden one does not — the settlement's building choices are a real fire-risk decision. A Flame DamageDef burns pawns through the existing health pipeline",
          "done": true
        },
        {
          "id": "health.fire.rain",
          "label": "Rain extinguishment is live: the weather module drives it through FireUtility.ExtinguishFiresFromRain, the path the fire lane ported in full and left waiting on a rain rate",
          "done": true
        },
        {
          "id": "health.corpse",
          "label": "Corpses: a pawn that dies is taken off the map and held by a Corpse rather than lingering spawned, so the chronicle, family tree and bereavement keep pointing at the same object. CompRottable drives Fresh/Rotting/Dessicated at a temperature-dependent rate; corpse ThingDefs are generated per race lazily, since DefLoader has no implied-def hook",
          "done": true
        }
      ]
    },
    {
      "id": "work",
      "n": 9,
      "rimworld": "Skills & Work",
      "translation": "Built: standing civilization-scale roles (RoleDef) replace a per-person priority grid. A role names the work types it emphasizes; Pawn_WorkSettings.ApplyRole sets those to the pawn's best priority (1) and leaves everything else at the default (3), rather than a player setting twelve numbers per citizen. Kept distinct from an EdictDef's own 'leaves no trace' guarantee (docs/spec/simworld-spec.md §10): a role is standing, not temporary, and does write into the priority grid — but Pawn_WorkSettings tracks every work type a caller set directly (SetPriority), and a role's own writes always skip those, so assigning or changing a role can never overwrite a person's own explicit choice; clearing a role (SetRole(null)) reverts every non-manual entry it touched back to default, the standing-policy mirror of an edict's guarantee. WorkPolicyUtility.ApplyRoleToPopulation is the civilization-scale lever — one call assigns a role across an entire settlement's citizens, the same 'act on the aggregate' shape GodManager gives edicts.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/Work · Pawns · Stats",
      "status": "translated",
      "tests": 86,
      "items": [
        {
          "id": "work.skills",
          "label": "SkillDef/SkillRecord/Pawn_SkillTracker: levels 0–20, per-pawn records in listOrder, daily xpSinceMidnight reset",
          "done": true
        },
        {
          "id": "work.xp",
          "label": "XP curve 1000→30000, passion 0.35/1/1.5, 4000 xp/day saturation, level up/down",
          "done": true
        },
        {
          "id": "work.decay",
          "label": "Unused-skill decay: levels 10+ lose 0.1–8.0 XP per 200-tick interval; below 10 never decays",
          "done": true
        },
        {
          "id": "work.disable",
          "label": "Work tags from traits disable skills and work types; backstory hook",
          "done": true
        },
        {
          "id": "work.priorities",
          "label": "Pawn_WorkSettings: priority grid per work type, 0–4, useWorkPriorities on/off collapse, disabled types forced to 0",
          "done": true
        },
        {
          "id": "work.givers",
          "label": "WorkGiverDef ordering: emergency first, priority → naturalPriority → priorityInType; scanning wired to real WorkGiver_Scanner subclasses by the AI module (system 9)",
          "done": true
        },
        {
          "id": "work.content",
          "label": "Core XML: 12 SkillDefs, 20 WorkTypeDefs, 27 WorkGiverDefs, Pyromaniac disables Firefighting",
          "done": true
        },
        {
          "id": "work.stats",
          "label": "Skill-driven stats: StatDef.skillNeedOffsets/skillNeedFactors (SkillNeed_Direct valuesPerLevel tables, SkillNeed_BaseBonus base+bonusPerLevel) resolved by StatWorker.GetValueUnfinalized off the pawn's own SkillRecord level, Class= polymorphic in content like this repo's other worker lists. Content: new WorkSpeedGlobal, MedicalTendQuality, MiningSpeed, ConstructionSpeed, CookSpeed and ResearchSpeed StatDefs, plus skillNeedOffsets added to the already-shipped ShootingAccuracyPawn/MeleeHitChance/MeleeDodgeChance. RimWorld's own curve numbers were not sourceable in this sandbox (see Stats_Work.xml's own remarks) — every curve here is this port's own invention, pinned by trend tests (higher skill gives a higher value, level 0 gives the plain base) rather than any literal.",
          "done": true
        },
        {
          "id": "work.backstory",
          "label": "Backstory-disabled work, landed end to end now that Pawn Generation exists (pawngen.backstory): BackstoryDef.workDisables/skillGains ship as content (Backstories_Civil/Tribal.xml, e.g. NobleChild/Scientist/TribalElder each disable real work), PawnGenerator assigns backstories and applies their skill gains, and Pawn.WorkTagIsDisabled/WorkTypeIsDisabled plus Pawn_WorkSettings consult them — a backstory-disabled work type is zeroed out of the priority grid and dropped from work-giver scanning exactly like a trait-disabled one, proven from this module's own side in WorkTests (pawngen.backstory's own tests prove it from pawn generation's side).",
          "done": true
        },
        {
          "id": "work.policy",
          "label": "Translation: policy/role-driven priorities at civ scale — RoleDef.emphasizedWorkTypes; Pawn_WorkSettings.SetRole/ApplyRole write emphasized types to top priority and everything else to default, skipping any work type a caller set directly (manualPriorities) so a role never overwrites a person's own preference and un-assigning one leaves no trace; WorkPolicyUtility.ApplyRoleToPopulation applies a role across a whole population at once. Four roles ship as content (Farmer, Miner, Artisan, Scholar).",
          "done": true
        },
        {
          "id": "work.repairplantscut",
          "label": "Repair and PlantsCut wired (28 of 29 work givers now carry a real giverClass). Both translate away a Designation system this port does not have: Repair scans any damaged artificial building because Thing carries no faction field, PlantsCut derives its targets from growing zones and construction cells",
          "done": true
        },
        {
          "id": "work.allwired",
          "label": "Every work type in content can produce a job except one. HaulCorpses, FightFires and CleanFilth were blocked on Corpse, Fire and Filth classes that did not exist; all three systems landed together rather than being stubbed. WardenDeliverFood is the single remaining bare giver, left deliberately because DoctorFeedHumanlikes reuses its mechanism with a different target filter",
          "done": true
        },
        {
          "id": "work.allgivers",
          "label": "Every WorkGiverDef in content now carries a real giverClass — 29 of 29. WardenDeliverFood was the last, and its recorded blocker had expired rather than been wrong: it needed a room concept to ask 'is there food in this prisoner's own room', and RoomTracker landed two batches later",
          "done": true
        }
      ]
    },
    {
      "id": "ai",
      "n": 10,
      "rimworld": "AI: Think Tree / Job System",
      "translation": "Implemented: think tree and jobs unchanged; the god issues edicts and goals that enter the tree above routine work instead of draft orders. Pathfinding is now shared for full-agent populations: AI.RegionPathCorridorCache builds one region-graph BFS tree per unique destination (paid once, reused by every pawn walking there, invalidated wholesale when RegionAndRoomUpdater.Version moves) and PathFinder tries a corridor constrained to it before falling back to its original unconstrained A* — a corridor can never be wrong (any doubt falls back to the always-correct search) but its region-hop-count metric is not cell distance, so a hierarchical route can be longer than optimal, disclosed and bounded by PathSharingTests rather than hidden. Measured (docs/perf/baseline.md §9): before/after speedup grows with population, from ~1.5-2.5x at N=100-1,000 to 9.7x at N=10,000 — an asymptotic (roughly O(N²) to O(N)) improvement for the 'N pawns to a shared destination' case, not just a constant factor. Hunting carries two civilization-scale translations of its own. There is no Designation system, so AI.HuntingInitiative decides for the settlement: hunt while short of food, stop when not — the shape SettlementConstructionInitiative already set. And there was no Corpse class when hunting landed, so the hunt's last toil butchers at the kill site through the shipped recipe and HaulGeneral carries the meat home, reaching RimWorld's end state by the only route this port has. Combat AI, which the port had none of: the whole Combat module was complete, tested and unreachable from play, because no JobGiver or JobDriver anywhere ever attacked and the humanlike think tree had no danger tier at all. Who fights is SimWorld's own, since there is no draft to port: anyone armed engages a hostile within acquire radius, anyone at all fights back within melee reach (RimWorld 1:1), and the new TakeUpArms edict raises an unarmed citizen to the first rule. Violence-disabled refuses regardless. A grudge stays personal rather than RimWorld's hostile-to-everyone manhunter, so a provoked animal charges its hunter and walks past everyone else.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/AI",
      "status": "ported",
      "tests": 136,
      "items": [
        {
          "id": "ai.thinktree",
          "label": "ThinkTree priority nodes (ThinkNode_Priority, ThinkNode_Conditional guards, ThinkNode_JobGiver leaves) + a real Humanlike ThinkTreeDef: danger/mental-state > needs > directed orders > routine work > idle wander",
          "done": true
        },
        {
          "id": "ai.jobs",
          "label": "Job + JobDriver toil state machine (Toil, ToilCompleteMode, Toils_General/Goto/Reserve, FailOn conditions) with Pawn_JobTracker driving it from Pawn.Tick",
          "done": true
        },
        {
          "id": "ai.reservations",
          "label": "ReservationManager per map: claim/release/CanReserve, released automatically on job end, Scribe round-trip",
          "done": true
        },
        {
          "id": "ai.regions",
          "label": "Reachability answers via BFS over the Map/RegionGrid graph (mapcore.regions) instead of its own flat flood-fill cache: a passability change now dirties and rebuilds only the region(s) it touches, not the whole map, and a query walks a handful of coarse regions rather than every cell on the shortest path. CanReachTarget/CanReach's public shape is unchanged",
          "done": true
        },
        {
          "id": "ai.pathing",
          "label": "A* (PathFinder) on the per-cell cost grid: array-backed binary min-heap open list, generation-stamped node info (no per-search clear), diagonal move with corner-cutting rules, pooled PawnPath, Pawn_PathFollower driving pawn movement at zero per-tick steady-state allocation",
          "done": true
        },
        {
          "id": "ai.pathing.sharing",
          "label": "Hierarchical path sharing over the Region/RegionLink graph: AI.RegionPathCorridorCache builds a BFS corridor tree once per unique destination region-set and shares it across every pawn walking there; PathFinder tries the corridor first and always falls back to its original unconstrained A* on any doubt, so a corridor can narrow a search but never make it wrong. Correct but not always optimal (hop-count corridor can be geometrically longer than the true shortest path — bounded by test, not hidden). Measured: before/after speedup on 'N pawns to a shared destination' grows from ~1.5-2.5x at N=100-1,000 to 9.7x at N=10,000 (docs/perf/baseline.md §9) — flow fields not attempted; the region-graph corridor already delivers the asymptotic win this item needed",
          "done": true
        },
        {
          "id": "ai.animals",
          "label": "A real second ThinkTreeDef (Animal) selected by RaceProperties.intelligence — JobGiver_AnimalFlee plus the shared eat/sleep/wander givers, no work/edict/directed-order tiers. Taming as a roll (TameUtility.TryTame) scored off the tamer's Animals skill against RaceProperties.wildness; failure can anger the animal at its tamer (Pawn_MindState.angryAt/tameness), rerouting its tree via ThinkNode_ConditionalAngryAtHandler instead of the Combat-module attack RimWorld's manhunter would trigger. Training as TrainableDef/TrainabilityDef + Pawn_TrainingTracker: steps accumulate per session and decay if untended past a grace window. WorkGiverDefs TameAnimals/TrainAnimals (already-shipped placeholders) wired to real scanners + JobDriver_Tame/JobDriver_Train.",
          "done": true
        },
        {
          "id": "ai.edicts",
          "label": "Translation: god edicts/goals as high-priority think nodes — built as JobGiver_Edicts, sitting between JobGiver_DirectedOrder and JobGiver_Work; see the god system's own entry for the full picture (EdictDef/EdictWorker, GodManager, the rollup)",
          "done": true
        },
        {
          "id": "ai.warden",
          "label": "The Warden work type wired to real scanners: WorkGiver_Warden_AttemptRecruit (WardenAttemptRecruit WorkGiverDef, JobDriver_Warden_AttemptRecruit + the PrisonerAttemptRecruit JobDef) drives the recruit/chat interaction combat.capture's Pawn_GuestTracker/WardenUtility already implemented but nothing called outside a test; WorkGiver_Warden_Feed (WardenFeed WorkGiverDef, JobDriver_Warden_Feed + FeedPatient) carries food to a downed, hungry prisoner and feeds it directly. WardenDeliverFood stays the WorkGiver_Pending placeholder — see that WorkGiverDef's own XML comment: this port has no room/prison-cell system, so a non-downed prisoner already reaches food entirely on its own via the ordinary JobGiver_GetFood tier, leaving RimWorld's ambulatory-prisoner DeliverFood case with nothing distinct to cover here. A downed hostile captured by CaptureUtility now ends up recruited (or its resistance worn down) purely through a colonist's own work-giver scan, not a utility called directly by a test.",
          "done": true
        },
        {
          "id": "ai.hauling",
          "label": "HaulGeneral wired to a real scanner: WorkGiver_Haul finds a spawned Item-category Thing (this port's own ThingDef.EverHaulable, since no separate alwaysHaulable/held-vs-loose distinction exists — nothing here can hold an item the way an equipment/apparel tracker would) not already sitting in a Zone_Stockpile cell whose ThingFilter allows it, and JobDriver_HaulToCell (HaulToCell JobDef) carries it to the nearest reachable, reservable stockpile cell with room (HaulAIUtility.TryFindBestStockpileCell) — nearest to the item, not the pawn, matching RimWorld's own StoreUtility. A destination cell already holding the same def merges the stack (count exactly summed); an empty one gets a freshly made Thing carrying the same Stuff. No matching stockpile (or every one already full) is an honest 'no job', not an invented dumping ground — this port has one storage kind and no priority tiers, so that is StoreUtility's whole job here. Carrying itself is modelled the same abstract way JobDriver_HaulToBuildingSite and JobDriver_Warden_Feed already do (no carry-tracker exists): the source stack's count drops the moment the pawn reaches it, nothing visibly follows the pawn to the stockpile. HaulCorpses (same WorkGivers.xml file) stays WorkGiver_Pending — no Corpse class exists anywhere in this codebase, so it cannot be wired honestly.",
          "done": true
        },
        {
          "id": "ai.rest",
          "label": "JobGiver_GetRest claims a real bed before falling back to the ground: AI.RestUtility.FindBedFor is a trim of RimWorld's own RestUtility.FindBedFor (nearest reachable, unclaimed Building.ConstructionThingDefOf.Bed — no owned/medical/prisoner bed distinctions exist yet, so every bed is open to anyone), reserved like any other claimed target (Toils_Reserve.Reserve) so two pawns never converge on the same one. JobDriver_LayDown walks to whichever target it got — a bed or the pawn's own position — and sets Need_Rest.lastRestEffectiveness from Need_Rest.BedRestEffectiveness (unsourced constant, chosen only to sit above the ground's own documented 1 and pinned by a test that a bed rests a pawn faster than the ground, never trusted as the literal) once there. No ownership/assignment UI — RimWorld's own CompAssignableToPawn — was built; a bed is first-come, matching what this port's Bed content actually needs.",
          "done": true
        },
        {
          "id": "ai.hunt",
          "label": "Hunt wired (WorkGiver_Hunt, JobDriver_Hunt, HuntingInitiative). Combat drives from a toil with no change to the Verb module; prey more than twice the hunter's BodySize is refused; an animal's revenge breaks off the hunt but it cannot fight back — no attack Job or JobGiver exists anywhere in this codebase",
          "done": true
        },
        {
          "id": "ai.hunt.dormancy",
          "label": "Shipped content can actually hunt: Tribesperson, the kind SettlementFounder generates every citizen as, carried no weaponTags, so no citizen in any generated game held a weapon and the Hunt giver was wired and permanently dormant. Guarded by a test that arms nobody",
          "done": true
        },
        {
          "id": "ai.combat",
          "label": "Pawns fight: JobGiver_AIFightEnemies above needs in both think trees, JobDriver_AttackMelee/AttackStatic, AttackTargetFinder, and natural weapons synthesised in code because no race in content defines tools. Hunted animals now fight back — the hunting lane left that open because no attack job existed",
          "done": true
        },
        {
          "id": "ai.combat.interrupt",
          "label": "Jobs can be interrupted. A constant think tree runs on a 30-tick hash interval whether or not a job is running, and JobDef.checkOverrideOnDamage plus Pawn_JobTracker.Notify_DamageTaken re-ask the main tree when a pawn is hit. The cadence is not a translation: RimWorld gates its own constant tree behind IsHashIntervalTick(30), so fidelity and affordability were the same choice — measured at 500 pawns on a real map, every-tick costs +123% of the pawn-tick budget and 1-in-30 costs +3.7%",
          "done": true
        },
        {
          "id": "ai.corpses",
          "label": "Corpses haul and butcher: WorkGiver_HaulCorpses, and butchery at a bench as a work giver rather than a bill — a bill's products are a fixed def+count list that cannot express a per-individual yield, and nothing in this port ever creates a bill, so bill-gated butchery would be content that can never fire",
          "done": true
        },
        {
          "id": "ai.combat.perf",
          "label": "Two measured wins found while benchmarking the above: AttackTargetFinder tested its expensive faction-hostility predicate before the cheap range check (1.98x per evaluation, and a raid used to make every citizen's evaluation dearer), and JobGiver_AIFightEnemies built a Verb before it had a target, allocating per evaluation to guard an empty set",
          "done": true
        },
        {
          "id": "ai.combat.targets",
          "label": "AttackTargetsCache: BestAttackTarget no longer walks the population. Spawned pawns are filed by faction on MapPawns plus a grudge list, so a searcher visits only hostile buckets — none in peacetime, and zero edits to Map.cs. The index is a superset and never an answer: every predicate still runs on every candidate, so only an omission could change behaviour, and the test asserts the cached scan against a whole-map scan over 40 random map states. Measured: the call stops reading population at all — about 10ns peacetime and 72ns under raid at any N, against 3ns x N before",
          "done": true
        }
      ]
    },
    {
      "id": "director",
      "n": 11,
      "rimworld": "Storyteller / Threat Director",
      "translation": "Translated: a chronicle-style narrator (name open — Scribe / Historian / The Chronicle) targeting the player's civilization. Threat points scale with civilization wealth, population and era instead of one colony.",
      "translationState": "proposed",
      "module": "src/SimWorld.Core/Director",
      "status": "ported",
      "tests": 125,
      "items": [
        {
          "id": "director.personality",
          "label": "Storyteller personas (Cassandra/Phoebe/Randy) built from comps: OnOffCycle, RandomMain, ClassicIntro, SingleMTB, Disease",
          "done": true
        },
        {
          "id": "director.points",
          "label": "Threat points: wealth and per-pawn curves × difficulty × adaptation × days-passed, clamped 35–10000",
          "done": true
        },
        {
          "id": "director.incidents",
          "label": "IncidentDef/category/target tags, workers (disease applies a hediff; threat/trade/wanderer placeholders), refire and gating rules, incident queue",
          "done": true
        },
        {
          "id": "director.adaptation",
          "label": "Quiet-time adaptation factor rising with peaceful days, dropping on downs and deaths",
          "done": true
        },
        {
          "id": "director.chronicle",
          "label": "Translation: narrator record (chronicle entries) and persona hook — name still open. Curated as history, not only a log: MomentCurator (owned deep-Scribed alongside the chronicle, like StoryWatcher_Adaptation/IncidentQueue) flags a chronicle entry a moment on first occurrence of its category (an incident's own defName, \"Death:<cause>\", or the text before a free-form line's first colon — no call-site changes needed), always for an era transition (spec §10: \"reaching an era is an event, not just a readout\"), and on a new longevity-at-death record; each rule is independently bounded in count so a routine century produces a handful of moments, never hundreds — see the quests system's own quests.moments item, which this mechanism belongs to.",
          "done": true
        },
        {
          "id": "director.raids",
          "label": "Raids resolve wherever they land. A raid on the settlement the god is watching spawns real pawns scattered from one edge cell and is fought on the map; a raid anywhere else resolves abstractly through SettlementRaidResolver — §11.3's 'real but not individually simulated' applied to an engagement rather than a person. Defence is priced in the same PawnKindDef.combatPower the raid is bought in, so the two sides are commensurable by construction; the Statistical cohort musters a share without ever being enumerated; casualties are bounded by what the enemy could inflict rather than by the defender's own size, so a city of 40,000 does not lose forty times a hamlet's dead to the same war band. Citizens die through FamilyManager.HandleDeath, the same path a death from age takes, so the chronicle and the family tree never disagree about who is alive",
          "done": true
        },
        {
          "id": "director.civ",
          "label": "Translation: era-based scaling — the current era multiplies threat points (standing in for the wealth term nothing computes yet), and IncidentDef.minEra/maxEra gate an incident to the ages it belongs to",
          "done": true
        },
        {
          "id": "director.multi-settlement",
          "label": "Translation: the storyteller targets a civilization of several settlements. CivilizationTarget stops being told what the civilization is and derives it from the player faction's own Settlements — the roster is their citizens, the seat is the oldest one (ties broken by tile, so list order never decides), and PlayerWealthForStoryteller is the market value of what they hold, the wealth term EraDef.threatPointsFactor was written as a stand-in for. An incident that has to happen somewhere picks a settlement weighted by population, never below weight 1 so an emptied town cannot vanish from the story, and a raid arrives at that settlement's interior map when someone has entered it. One StoryState still covers the whole civilization: a raid this decade is a fact about the civilization, not about a town. Fixes in passing a roster that counted every citizen on the planet, rival civilizations included, once EmergenceManager started founding them",
          "done": true
        },
        {
          "id": "director.raids.unwatched",
          "label": "Measured before it was fixed: a raid on an unwatched settlement generated twelve raiders, spawned none, changed no population and no stores, and still wrote 'RaidEnemy fired' to the chronicle. Target selection is deliberately left alone — biasing raids toward the watched settlement would make the bug rarer rather than gone, and the tiering exists so unwatched settlements stay real",
          "done": true
        },
        {
          "id": "director.chroniclefame",
          "label": "Notify_ChronicleNamed has a policy at last: a living citizen is named when they outlive every life the civilization has ever known. It is a ratchet, so it fires less often the longer a game runs; it cannot be earned by arriving, working or being looked at; and at most one living citizen holds it at a time. Measured over a century of real demography: three to four citizens ever named, peak one alive",
          "done": true
        },
        {
          "id": "director.romancemoments",
          "label": "Romance was eating the landmark list: it wrote free-form lines with no category prefix, so every romance and divorce read as a first of its kind and was appended forever to MomentCurator.Moments, which is never trimmed, at a rate that grows with population",
          "done": true
        },
        {
          "id": "director.difficulty",
          "label": "Choosing a difficulty now does something. Eleven DifficultyDef fields existed and four were wired; six of the seven dormant ones are now consumed — allowBigThreats, allowIntroThreats, colonistMoodOffset, cropYieldFactor, questRewardValueFactor and researchSpeedFactor — all through one DifficultyUtility so the null-difficulty and never-negative handling is written once",
          "done": true
        },
        {
          "id": "director.difficulty.peaceful",
          "label": "Peaceful never suppressed raids. DefaultThreatPointsNow clamps up to MinThreatPoints (35), which is exactly RaidEnemy's own minThreatPoints floor, so a threatScale of 0 did not turn threats off — it shrank every raid to the smallest legal war band and fired it on schedule. The clamp is RimWorld's and stays; allowBigThreats is how RimWorld turns the category off",
          "done": true
        },
        {
          "id": "director.difficulty.auditgap",
          "label": "The wiring audit has a blind spot this lane fell into: its field checks ask 'content sets it, does code read it' and 'code reads it, does content set it', so a field neither side touches is reported by neither. researchSpeedFactor was exactly that and appears nowhere in the baseline",
          "done": false
        },
        {
          "id": "director.downedadaptation",
          "label": "StoryWatcher_Adaptation.Notify_ColonistDowned is raised — Pawn_HealthTracker's downing path now tells the narrator (src/SimWorld.Core/Director/StorytellerPawnEvents.cs), so adaptation eases after a mauling and not only after a funeral. Same shape as the Notify_ColonistDied hook this audit was written after.",
          "done": true
        }
      ]
    },
    {
      "id": "combat",
      "n": 12,
      "rimworld": "Combat",
      "translation": "Unchanged mechanics; armies are groups of full agents.",
      "translationState": "proposed",
      "module": "src/SimWorld.Core/Combat · Map · Building · Factions · Health/Damage (armor)",
      "status": "ported",
      "tests": 48,
      "items": [
        {
          "id": "combat.verbs",
          "label": "Verbs: warmup → burst → cooldown state machine; ranged shot reports (accuracy^distance, weapon bands 3/12/25/40, target size, cover) and melee hit/dodge from skill curves",
          "done": true
        },
        {
          "id": "combat.damage",
          "label": "Armor: DamageArmorCategoryDef (Sharp/Blunt/Heat), ArmorUtility deflect/halve rolls, Sharp→Blunt diminish, penetration; wired into DamageWorker_AddInjury",
          "done": true
        },
        {
          "id": "combat.weapons",
          "label": "Weapon and projectile content: knife, club, bow, revolver, assault rifle; tool capacities and maneuvers",
          "done": true
        },
        {
          "id": "combat.cover",
          "label": "Cover: CoverInfo/CoverUtility wired to real map geometry — Map.GenSight (Verse.GenSight-style Bresenham line of sight, decompiled-source-verified) gates a shot, CoverUtility.CalculateCoverGiverSet (the real 8-adjacent-cell, angle- and distance-weighted algorithm) resolves automatically from caster/target position once both are spawned on a map; a pawn behind a wall is measurably harder to hit than one in the open",
          "done": true
        },
        {
          "id": "combat.structures",
          "label": "Turrets (CompTurretGun: hostile-pawn scan throttled to a 15-tick hash interval, fires the existing Verb/ShotReport pipeline — caster widened from Pawn to Thing for this), traps (CompTrap: armed, springs once on the first non-owning-faction pawn to enter, then spent), explosions (Combat.GenExplosion: GenExplosion/DamageWorker_AddInjury-style radial cells-to-hit with line-of-sight wall blocking, optional linear damage falloff) — all plain ThingDefs with comps, the Building module's existing shape rather than RimWorld's own distinct Building_TurretGun/Building_Trap subclasses",
          "done": true
        },
        {
          "id": "combat.capture",
          "label": "Downed → captured → prisoner loop: CaptureUtility (downed + hostile-faction → Pawn_GuestTracker) and WardenUtility (resistance reduction → recruit, or release), GuestStatus/PrisonerInteractionModeDef kept as RimWorld's own names. Pawn_GuestTracker lives on the host Faction (Faction.prisoners), not on Pawn — Pawns/** is another lane's module and carries no such field; see Pawn_GuestTracker.cs's own remarks. The Warden WorkGiver/JobDriver that sends a colonist to actually carry this out is AI/Work content and code — see the ai system's own ai.warden entry.",
          "done": true
        }
      ]
    },
    {
      "id": "crafting",
      "n": 13,
      "rimworld": "Crafting & Production",
      "translation": "Proposed: bills unchanged; guilds and industries act as bill queues at scale.",
      "translationState": "proposed",
      "module": "src/SimWorld.Core/Crafting",
      "status": "ported",
      "tests": 58,
      "items": [
        {
          "id": "crafting.bills",
          "label": "Bills and BillStack: repeat modes (count, target with hysteresis, forever), skill gating, suspend, ordering",
          "done": true
        },
        {
          "id": "crafting.ingredients",
          "label": "Ingredient selection by value getter over candidate stacks, splitting and mixing rules — stockpile scanning lands with AI",
          "done": true
        },
        {
          "id": "crafting.quality",
          "label": "Skill-driven quality generation with inspiration; trader and base-gen rolls",
          "done": true
        },
        {
          "id": "crafting.cooking",
          "label": "Cooking recipes, skill-driven food poisoning, eating and nutrition",
          "done": true
        },
        {
          "id": "crafting.filters",
          "label": "ThingFilter over a ThingCategoryDef tree, with stuff categories, quality and hit-point ranges",
          "done": true
        },
        {
          "id": "crafting.animals",
          "label": "Animal husbandry: RaceProperties.meatDef/leatherDef feed Recipe_ButcherAnimal (isButchery RecipeDef, applied straight to a dead animal pawn like a surgery — no Corpse thing exists, so ButcherUtility.TryButcher is the same no-job-driver call shape SurgeryUtility already established), yield scaled by body size (HusbandryTuning). CompMilkable/CompShearable/CompEggLayer (shared CompHasGatherableBodyResource base) fill toward 1 over a per-species interval and spawn real Milk/WoolAnimal/EggAnimalUnfertilized items on Gather. New content: Muffalo (milk+wool) and Chicken (eggs) beyond the already-shipped Husky, so wildness/produce variety is real.",
          "done": true
        },
        {
          "id": "crafting.guilds",
          "label": "Translation: guilds and industries as bill queues at civilization scale. A GuildDef names the RoleDef that staffs a guild, the skill its craftsmen are measured by and the recipes it may queue; a Guild holds a real BillStack — repeat modes and target-count hysteresis unchanged — but belongs to a Settlement, draws fixed ingredients from that settlement's Stores ledger and puts products back into it. Members are the settlement's citizens carrying the role, plus an assigned share of the Statistical cohort whose skill is one deterministic cohort sample, GodRollup's own idiom. Labour banks across intervals (WorkAccumulated) so a batch costing more than one interval can be finished at all, and a guild with no runnable bill banks nothing. Not modelled, all for the same reason — it belongs to the settlement interior: no workbench Thing, no hauling, no job driver. A recipe whose ingredients are a category filter is refused in ConfigErrors rather than silently skipped: a def-count ledger has no stockpile to choose between candidates in",
          "done": true
        },
        {
          "id": "crafting.workbenches",
          "label": "Bills at a workbench, map-scale: CompBillGiver (Things/CompBillGiver.cs) makes a spawned Building an IBillGiver by composition, following the Building module's own comp-over-subclass grain (CompPower/CompTurretGun/CompTrap); CompProperties_BillGiver.workType is matched against a WorkGiverDef's own workType so one WorkGiver_DoBill (Crafting/WorkGiver_DoBill.cs) serves DoBillsSmith/DoBillsTailor/DoBillsArt/DoBillsCraft/CookMeals instead of five near-copies — RimWorld's own WorkGiverDef.fixedBillGiverDefs would have needed a shared Work/WorkGiverDef.cs edit this pass avoided. JobDriver_DoBill spends skill-scaled work ticks, then re-resolves ingredients near the bench through the existing BillIngredientsFinder, consumes the real map Things, spawns the product via GenRecipe/ThingMaker and grants skill XP. Ingredient delivery to the bench is deliberately out of scope — mirrors WorkGiver_ConstructFinishFrame/ConstructDeliverResourcesToFrames' own split; a bill only runs once ingredients already sit within WorkGiver_DoBill.IngredientSearchRadius, which HaulGeneral's WorkGiver_Haul can stock via an adjacent stockpile zone. Content: FueledStove and TableStonecutter (existing benches) and two new ones, Smithy and TableTailor, each carrying the comp; two new recipes (Make_MeleeWeapon_Knife, Make_Apparel_Shirt). DoBillsArt ships wired with no bench yet — a Sculpture/ArtDef content system is its own piece of work, not a mechanism gap.",
          "done": true
        }
      ]
    },
    {
      "id": "research",
      "n": 14,
      "rimworld": "Research & Tech",
      "translation": "Translated: a large authored tech tree detailing societal evolution across eras. Data model leaves room for procedural/endless tech later.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Research · Data/Core/Defs (tabs, eras, projects) · tools/content/gen_techtree.py",
      "status": "ported",
      "tests": 93,
      "items": [
        {
          "id": "research.dag",
          "label": "Research project DAG: prerequisites, hidden prerequisites, cycle detection",
          "done": true
        },
        {
          "id": "research.progress",
          "label": "Research manager: progress, tech-level cost factor (+50% per level above), completion events, Scribe",
          "done": true
        },
        {
          "id": "research.unlocks",
          "label": "IResearchUnlockable and ResearchProjectDef.UnlockedDefs: the mechanism that computes what a project unlocks from every def that names it",
          "done": true
        },
        {
          "id": "research.unlocks-content",
          "label": "Content wired to it: ThingDef and RecipeDef implement IResearchUnlockable, and the buildings, weapons and recipes that exist are gated on projects that exist — a stove on Cooking, a stonecutter's table and sandstone blocks on Masonry, a bow on Bows, pemmican on FoodPreservation. A club stays ungated: a stick is not a technology.",
          "done": true
        },
        {
          "id": "research.era-spine",
          "label": "Eras turn on their spine (the projects something else depends on) rather than on 100% of their content, so a dead-end flavour project cannot bar the next age",
          "done": true
        },
        {
          "id": "research.techlevel",
          "label": "ResearcherTechLevel rises with the era instead of being set once at scenario start — it was silently taxing the whole tree 2.5x from a tribal start",
          "done": true
        },
        {
          "id": "research.era-transitions",
          "label": "Entering an era is an event: fired once per era crossed (derived from before/after the project finish, so loading a save never re-announces history), it writes a chronicle line and a letter, and a scenario’s starting era is seeded silently rather than narrated",
          "done": true
        },
        {
          "id": "research.horizon",
          "label": "Pacing, re-framed after the horizon decision: years are not the currency, so \"can a 50-year civilization have everything\" is a question about a unit that no longer applies. Measured per era instead — how much of an era a civilization has seen when it leaves it, and whether two archetypes see the same things. Answer: seen% lands within 2-4 points of the era’s spine share in every era, so cost tuning moves how MUCH gets done and never WHAT, and overlap never falls below ~0.85 at any price. Do not re-price the tree (docs/research/tech-reachability.md §10)",
          "done": true
        },
        {
          "id": "research.divergence",
          "label": "Divergence authored into the tree, not priced for: gen_techtree.py was restructured (68 prerequisite edges de-linearized, protecting the 22-node deep chain and every same-or-earlier-era/no-cycle invariant) and given 13 new leaf projects in previously all-spine track/era cells. Spine fraction fell from 48-85% (median ~72%) to 28-54% (median ~41%) per era, and the harness's eras command shows seen% and archetype overlap measurably lower in every era at the shipped throughput — see docs/research/tech-reachability.md §11",
          "done": true
        },
        {
          "id": "research.eras",
          "label": "Translation: EraDef ladder (Sticks & Stones → Exotic) with per-era progress and current/next era",
          "done": true
        },
        {
          "id": "research.content",
          "label": "Core XML: research tab, 8 eras, a 30-project seed tree across the first eras",
          "done": true
        },
        {
          "id": "research.tree",
          "label": "Authored tech tree of societal evolution: 245 projects across 8 eras and 14 tracks, generated from tools/content/gen_techtree.py",
          "done": true
        },
        {
          "id": "research.endless",
          "label": "Translation: procedural tech past the authored tree, endless in fact rather than in name. A generated label is {age register} {track substrate} {form} — 'holographic attention first principles' — drawn from an EndlessAgeDef's themes and an EndlessResearchDef's substrates and forms, which are disjoint per track and per role, so distinctness within an age is structural rather than checked. Age to theme is injective for the first 20 ages and then compounds with particles (post-holographic, meta-post-granular) — a base-8 numeral written in words, so the supply has no ceiling. Six tracks x 4 slots per age: one foundation, three optional, the same minority-spine shape the authored tree uses. Every field is a pure function of (world seed, track, age, slot) through seeded affine permutations and never reads Rand.Current, so the nth project is nameable without the first n-1 and a save stores two integers. Cost is polynomial, not geometric: depth 20 fell from 1,796,772 points (three times the entire authored tree) to 95,454. Generated projects carry no era and cannot hold the ladder open — it is a finite authored artefact ending at Exotic, and ages are endless research's own grouping, announced by their own letter and chronicle line",
          "done": true
        },
        {
          "id": "research.work",
          "label": "The Research WorkTypeDef gets a real worker: WorkGiver_Research (a WorkGiver_Scanner, Research's own giverClass in WorkGivers.xml) scans for a reachable, unclaimed ResearchBench and issues JobDriver_Research, which walks to it and every tick adds ResearchManager.ResearchPointsPerWorkTick * pawn.GetStatValue(StatDefOf.ResearchSpeed) to CurrentProj through the real ResearchManager.ResearchPerformed — the ResearchSpeed StatDef's first real consumer, replacing the stand-in speed(skill) curve docs/research/tech-reachability.md had to model by hand. A bench is required, matching RimWorld: none built, or none reachable/unclaimed, and the giver simply finds no job; ResearchBench (Data/Core/Defs/ThingDefs_Buildings/Buildings_Research.xml) carries no researchPrerequisites of its own, so this can never be a lock a civilization cannot build its way out of, and it is buildable through the ordinary Blueprint/Frame/GenConstruct pipeline like any other building. ShouldSkip short-circuits the scan when CurrentProj is null; a project finishing mid-toil ends the job Succeeded the same tick rather than leaving the pawn idling with nothing current. Also grants Intellectual xp per tick (0.1, RimWorld's own JobDriver_Research value, named in tech-reachability.md's own assumption table)",
          "done": true
        },
        {
          "id": "research.endless.perseed",
          "label": "Two civilizations no longer share one tail. The generated tree was a pure function of content, stated as a virtue, which meant no two games ever diverged past the ladder; it is now derived from the world seed. The same change fixed a real bug: the process-wide DefDatabase let a second game in one process read the first game's unfinished generated projects as startable, so it never extended past the authored tree at all",
          "done": true
        },
        {
          "id": "research.endless.indexceiling",
          "label": "Literal bound on 'endless', recorded not fixed: DefDatabase.Add narrows Def.index with checked((ushort)(count-1)), so a process minting more than 65,535 ResearchProjectDefs throws. About 2,700 ages in, each project around 4e8 points — unreachable in play, but it is a ceiling and it is not in the design",
          "done": false
        }
      ]
    },
    {
      "id": "economy",
      "n": 15,
      "rimworld": "Economy, Trade & Factions",
      "translation": "Decided: other civilizations are the factions (unchanged); trade routes between settlements replace RimWorld's orbital/caravan traders as the connective tissue, priced the same way CoalSupply already prices coal (a real path through the world pather, marked up by distance) and closed while the two settlements' factions are at war; goodwill becomes diplomacy through two new layers RimWorld has no equivalent of — an explicit War/Peace state per relation (Faction.DeclareWar/MakePeace, entry and exit acts rather than goodwill silently crossing a threshold) kept separate from the existing Hostile/Neutral/Ally goodwill-derived label, and Def-driven Treaty terms (non-aggression, trade access) a pair of factions can sign, each with its own duration and goodwill effect. Every number here (war/peace goodwill swings, route premium, treaty terms) is this port's own judgement call, not sourced from RimWorld — see the code's own comments for where and why.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Factions · Economy · Caravans",
      "status": "ported",
      "tests": 132,
      "items": [
        {
          "id": "economy.goodwill",
          "label": "Faction relations: goodwill clamped to ±100, hostile/neutral/ally thresholds, permanent enemies, initial relations and natural drift",
          "done": true
        },
        {
          "id": "economy.currency",
          "label": "Market value as the trade unit with PriceType tiers",
          "done": true
        },
        {
          "id": "economy.pricing",
          "label": "Buy and sell pricing with a negotiator Social-skill gain curve",
          "done": true
        },
        {
          "id": "economy.caravans",
          "label": "Caravans: world path grid and finder over hilliness, biome and roads; ticks per move from the slowest pawn; food drain and arrival",
          "done": true
        },
        {
          "id": "economy.coal-access",
          "label": "CoalSupply: a settlement's access to coal, local deposit or the cheapest reachable settlement that has one via the caravan pathfinder, priced through the existing TradeUtility buy markup plus a route-distance premium — the predicate/cost a later era-transition or industrialisation check reads",
          "done": true
        },
        {
          "id": "economy.pawn-groups",
          "label": "FactionDef.pawnGroupMakers: PawnGroupKindDef/PawnGenOption/PawnGroupMaker plus FactionDef.GetGroupMaker — the squad-composition roster director.raids spends threat points against; each raiding FactionDef's own maker list is the tech gate (a neolithic faction's maker never lists an industrial PawnKindDef)",
          "done": true
        },
        {
          "id": "economy.traders",
          "label": "Traders: TraderKindDef with trade sessions and deals — stock generation and visiting traders land with Things/AI",
          "done": true
        },
        {
          "id": "economy.diplomacy",
          "label": "Translation: civilization diplomacy — treaties, wars and trade routes between civilizations",
          "done": true
        },
        {
          "id": "economy.billreservation",
          "label": "Bills reserve their chosen ingredient stacks, not just the bench: Job target queues, ReservationManager stack counts and Toils_Reserve.ReserveQueue ported from RimWorld, so two crafters can no longer both spend one pile",
          "done": true
        }
      ]
    },
    {
      "id": "building",
      "n": 16,
      "rimworld": "Colony Building & Environment",
      "translation": "Implemented: unchanged mechanics; settlements grow from citizen initiative plus god edicts rather than direct placement of every wall — SettlementConstructionInitiative decides what a settlement needs and places the blueprint, and an active edict (EdictDef.prioritizedConstruction) biases which unmet need gets filled first; see building.initiative.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/Building",
      "status": "translated",
      "tests": 66,
      "items": [
        {
          "id": "building.zones",
          "label": "Zone/Zone_Stockpile/Zone_Growing/ZoneManager (one zone per cell, Scribe round-trip with cells) plus a map-wide Area/AreaManager for the home area, kept distinct from Zone per RimWorld's own split; WorkGiver_GrowerSow (cell-scanning) sows an active growing zone's empty cells, WorkGiver_GrowerHarvest harvests any harvestable-now Plant map-wide — Zone_Stockpile's ThingFilter is now a real hauling destination too: HaulGeneral is wired (system 9: AI — see ai.hauling) to AI.WorkGiver_Haul/AI.JobDriver_HaulToCell, which read Zone.cells and Zone_Stockpile.filter directly rather than through any new member on this class",
          "done": true
        },
        {
          "id": "building.construction",
          "label": "Blueprint → frame → building via real AI jobs/toils/reservations: GenConstruct placement validity, WorkGiver_ConstructDeliverResourcesTo{Blueprints,Frames} haul materials (ThingDefCountClass costList), WorkGiver_ConstructFinishFrame spends work scaled by Construction skill (read directly, not via StatDef.skillNeedFactors — see this system's report) and rolls success/fail; a failed Frame refunds half its delivered materials and respawns a Blueprint",
          "done": true
        },
        {
          "id": "building.roof",
          "label": "RoofCollapseUtility: a roofed, edifice-free cell needs a Fillage.Full edifice (wall or unmined rock) within a straight-line radius; losing its last one collapses it, removing the roof and dealing Blunt damage (more for a thick natural roof) to whatever is under it. Event-driven off Map.EdificeGrid.DeRegister, not a per-tick scan; built on neither the region graph nor Room/RoomGroup — both answer a different question (reachability partitioning / thermal enclosure) than \"how far is the nearest wall\" — see this system's report for the full reasoning. DestroyMode.WillReplace (declared, previously unused) stops Frame.CompleteConstruction's Frame→Building swap from reading its own momentary despawn as support lost",
          "done": true
        },
        {
          "id": "building.power",
          "label": "CompPower/CompPowerTransmitter/CompPowerTrader/CompPowerPlant/CompPowerBattery; PowerNetManager builds/merges/splits PowerNets incrementally off cardinal adjacency as comps spawn/despawn (never a full-map rebuild); brownout is a real RimWorld whole-net simultaneous cutoff once batteries are exhausted, not the brief's described per-trader priority order (see this system's report)",
          "done": true
        },
        {
          "id": "building.temperature",
          "label": "Room/RoomGroup flood-filled from the edifice grid (a Door still splits a Room but RoomGroup re-merges rooms joined only by one); per-RoomGroup temperature equalises toward Map.outdoorTemperature at a rate from boundary Insulation and cell count, pushed by CompHeatPusherPowered; no biome/season modulation of outdoorTemperature exists (it is a flat settable field — no biome/season/climate system to drive it yet)",
          "done": true
        },
        {
          "id": "building.plants",
          "label": "Building.Plant grows on the long tick at fertility (TerrainDef.fertility) × light × temperature — RimWorld's own three-factor product; light derived from GenDate's day/night clock (no per-map longitude exists yet, so every map shares one clock — see this system's report) and temperature from a recalled-not-verified RimWorld curve (0 below freezing/above 58°C, full rate in a 10-42°C band); harvest yields ThingDef.plant.harvestedThingDef scaled by Growth. Content ships two sowable crops (Plant_Potato, Plant_Rice) plus WildPlant upgraded to the same real Plant class",
          "done": true
        },
        {
          "id": "building.initiative",
          "label": "Translation: citizen-initiated construction under edicts — SettlementConstructionInitiative reads a settlement's real Citizens count (never StatisticalPopulation: a Statistical citizen has no individual Pawn to physically house, by the tiering system's own design — see the class's own remarks), what is already built or already planned on its InteriorMap, and what its Stores ledger holds, then derives a small, concrete need list (a Bed per citizen, a handful of Wall blueprints once there is anyone to shelter, a StorageHut once Stores holds enough to want one) and places blueprints for the shortfall through the existing GenConstruct/Blueprint/Frame pipeline — nothing decided to place a blueprint before this pass. Placement samples cells at random off the ambient RandomStream rather than scanning the map, validated entirely by GenConstruct.CanPlaceBlueprintAt (never overlapping or blocking what is already there), throttled to ConstructionInitiativeTuning.MaxBlueprintsPerTick per gated call so a settlement with a large shortfall grows over many ticks rather than flooding its map at once. Self-gated on GenTicks.TickRareInterval, the same 'rare bucket, not a per-tick scan' shape GodManager.GodTick/Storyteller.StorytellerTick/Settlement.GrowthTick already use; a settlement nobody has entered has no InteriorMap and this is a deliberate no-op there, never a triggered generation. The edict seam: EdictDef.prioritizedConstruction (new field, same declarative/read-live shape as prioritizedWork) reorders which unmet need a settlement fills first while an edict naming it is active — GreatWorksMandate ships it as real content ('quarry and building site before anything else') — with no activation-time side effect, so deactivating leaves no trace. New buildable content: Bed and StorageHut ThingDefs with Blueprint/Frame pairs alongside the existing Wall. Bed is now wired into Need_Rest/JobGiver_GetRest (AI.RestUtility.FindBedFor, AI.JobDriver_LayDown — see ai.rest): a citizen actually claims and sleeps in the bed this system builds for them, resting faster than the ground. SettlementConstructionInitiative.Tick() is wired into Sim/Game.cs's WireTickHooks as a normal PostTicker, the civilization-wide entry point a host loop calls once per tick (mirroring God.GodTick()); a settlement's own Full-tier citizens are what actually walk over and build what it queues, once Settlement.SyncCitizenSpawns (settlements.citizen-presence) puts them on its interior map in the first place.",
          "done": true
        }
      ]
    },
    {
      "id": "social",
      "n": 17,
      "rimworld": "Social, Ideoligion & DLC Systems",
      "translation": "Opinions and social interactions ported for this pass, RimWorld's compatibility-factor mechanic (a stable hash of the two pawns) included; ideoligions become the religions and ideologies of your civilization — memes/precepts feeding thoughts through the existing pipeline, rituals with a quality resolution, and precept-granted roles are built (social.ideology); per-citizen ideoligion membership (certainty, conversion, an outsider penalty) is not, and one Ideo stands in for a whole civilization's belief for now — see that item's own label. DLC systems (genes, psycasts, anomalies), still proposed, evaluated per era. RimWorld's Thought_Memory/Thought_MemorySocial split is folded into one Thought_Memory class with a nullable otherPawn and a ThoughtDef.IsSocial property, rather than a second thought class; RimWorld's spatial \"a friend is nearby\"/\"a rival is present\" situational thoughts read this port's stored relations instead (no Map/room concept reaches Social or Thoughts yet), via one generic, content-driven ThoughtWorker_HasDirectRelation.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/Social · Social/Ideology",
      "status": "ported",
      "tests": 62,
      "items": [
        {
          "id": "social.opinion",
          "label": "Opinion score (Pawn_RelationsTracker.OpinionOf) from relation type (family relations derived from demography's own ids; friend/rival/lover/ex-spouse stored as DirectPawnRelation), social memories about that specific pawn, personality traits (TraitDegreeData.opinionOffset), and a stable per-pair compatibility factor",
          "done": true
        },
        {
          "id": "social.interactions",
          "label": "Periodic interaction rolls on a 2,500-tick population sweep, selected by weight per pair (chitchat, deep talk, insult, slight); a low-opinion, low-mood insult can escalate into a social fight via the existing MentalStateDef machinery — MentalState_SocialFighting runs on both participants, cross-linked via otherPawn, and actually trades blows through Combat's own Verb_MeleeAttack/MeleeVerbUtility path, ending at the Def's own duration/MTB recovery or the instant either side goes down",
          "done": true
        },
        {
          "id": "social.romance",
          "label": "Courtship and its ending. Two more InteractionDefs rather than a sweep of their own: InteractionWorker_RomanceAttempt weighs zero for anyone married, related, too young, already together or not liked enough, and acceptance turns on opinion plus the pair's stable compatibility factor; InteractionWorker_Breakup is offered only to a couple whose social memories of each other have gone negative — measured on memories alone, because total opinion also carries the Lover relation's own +20 (a couple happy because they are a couple) and the compatibility factor (two people who click could never fall out). A marriage ends as a divorce: both spouse ids cleared and a mutual ExSpouse recorded, the shape widowhood already used, leaving the household standing because a household here is a lineage rather than a residence. Deliberately absent: infidelity (a married citizen never courts) and orientation (no pawn carries one, and a gate the data cannot support would be invented)",
          "done": true
        },
        {
          "id": "social.ideology",
          "label": "Built: MemeDef/PreceptDef (memes are broad themes with slots; precepts are the specific rules, faithfully split), an Ideo runtime object generated from an IdeoDef preset and Scribe round-tripped, precepts feeding mood through the existing thought pipeline (ThoughtWorker_UnderPrecept, built directly on the ThoughtWorker_UnderEdict precedent — one PreceptWorker class serves opposing judgments, the mood sign lives in content), rituals with a quality resolution (RitualUtility: participant count, officiant-role presence, participant mood — the shape is RimWorld's, the numbers are this port's own, pinned by trend tests since RimWorld's own quality table could not be sourced here), and precept-granted roles (IdeoRoleDef, capped by maxHolders — a second concept from Work.RoleDef, not an extension of it, partly because the two class names would silently collide in Def-type resolution). Content ships two contrasting ideoligions (TheHearthway, TheForgeCovenant) whose precepts read the same citizen state and reach opposite mood conclusions, plus a ritual and a role each, working end to end. NOT built: per-citizen ideoligion membership (RimWorld's certainty/conversion/outsider-penalty) — one Ideo stands in for a whole civilization's belief, every Humanlike citizen assumed a member, which is exactly the much-larger design this item originally deferred; event-tracked precepts (cannibalism, corpse treatment) — no consumption/corpse event stream exists to read, so every shipped precept instead reads state another module already tracks durably (a trait, worn apparel, a role). IdeoManager is a thread-static self-gate in Social/Ideology, not a Find.Ideo — Sim/** was out of this pass's file ownership; wiring a real Find.Ideo/Game.Ideo slot is the follow-up.",
          "done": true
        },
        {
          "id": "social.dlc",
          "label": "Genes, mechanitor bandwidth, psycasts, containment — evaluated per era",
          "done": false
        }
      ]
    },
    {
      "id": "quests",
      "n": 18,
      "rimworld": "Quests & Scenario",
      "translation": "Built: scenarios become civilization starts (era, biome, rivals — quests.starts); quests become narrator-issued historical moments (quests.moments). A moment is the narrator noticing something worth remembering — a first, a record, a turning point — rather than a line per routine event; the Chronicle already recorded births, deaths, edicts and era transitions unconditionally, so the missing piece was deciding which of those matter more. Built as MomentCurator, owned by the Storyteller (src/SimWorld.Core/Director) since that is where the Chronicle itself lives — see that system's director.chronicle item for the mechanism and its own report for why the code sits there rather than under Quests.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/Quests · Scenario · Letters (quests.moments' curator lives in src/SimWorld.Core/Director — see director.chronicle)",
      "status": "translated",
      "tests": 46,
      "items": [
        {
          "id": "quests.scenario",
          "label": "ScenarioDef and ScenPart: starting conditions, forced traits, starting research and things",
          "done": true
        },
        {
          "id": "quests.scripts",
          "label": "QuestScriptDef node graphs over a Slate: sequence, set, branch, delay, letter, reward, end — compiled into quest parts with unique signals",
          "done": true
        },
        {
          "id": "quests.delivery",
          "label": "Letters and LetterStack with accept/expire timers; the director's GiveQuest incident offers by points, weight and refire days",
          "done": true
        },
        {
          "id": "quests.starts",
          "label": "Translation: civilization starts — era, rival-civilization count and biome scenario parts",
          "done": true
        },
        {
          "id": "quests.moments",
          "label": "Translation: narrator-issued historical moments — MomentCurator (src/SimWorld.Core/Director/MomentCurator.cs, owned deep-Scribed by Storyteller) flags a chronicle entry as a moment on the first occurrence of its category, always for an era transition, and on a new record for longevity at death; a moment death also promotes the pawn via Pawn_TierTracker.Notify_ChronicleNamed. Each rule is independently bounded in count (by distinct categories, by the fixed era ladder, or by a monotonic ratchet), pinned by test that a routine simulated century produces far fewer than a hundred moments.",
          "done": true
        },
        {
          "id": "quests.orchestrator",
          "label": "Game-start orchestrator to run Scenario.PostGameStart and the quest tick loop — Sim/Game.cs: Game.NewGame founds the settlement then runs PostGameStart against its citizens, and QuestManager.QuestManagerTick is wired into the post-tick order (it had never been driven by anything before)",
          "done": true
        },
        {
          "id": "quests.rewardsink",
          "label": "Quest rewards reach the civilization — QuestRewardSink (src/SimWorld.Core/Quests/QuestRewardSink.cs) is the implementation IQuestRewardSink never had, installed on every QuestManager. Before it, QuestPart_Reward.Enable handed its records to a null interface: a completed Quest_LostCaravan reported '200 silver, 5 goodwill' and the seat's stores, the civilization's wealth and the faction's goodwill were all unchanged. Silver banks into CivilizationTarget.Seat's ledger (not the settlement the player is watching, and with no roll at all, since this runs inside QuestManagerTick); goodwill moves the named faction's relation; anything unpayable is counted and said in the chronicle rather than dropped.",
          "done": true
        },
        {
          "id": "quests.rewardvaluefactor",
          "label": "DifficultyDef.questRewardValueFactor applies once, at generation (QuestNode_GiveReward), not at payout. Two lanes wired it in the same batch — one at each end — and the merge caught it before a 0.8 difficulty could pay 0.64. Generation is RimWorld's end and also the value Quest.RewardsSummary advertises, so what a quest promises and what the treasury receives are the same number; the sink keeps only the floor that stops a positive award rounding to nothing.",
          "done": true
        },
        {
          "id": "quests.tribute",
          "label": "A tribute demand that can be answered. Quest_Tribute shipped for four batches as a script that set demandSilver from the threat points, sent a letter, waited out the offer and ended Fail — always, identically, whether the civilization was rich or destitute. Nothing read demandSilver, no coin moved either way, and refusing cost nothing, so 'or threatens reprisal' in its own description had no mechanism behind it: the exact mirror of the reward-sink defect, with value unable to leave the civilization as it had been unable to enter. QuestPart_Tribute answers out of the same seat a reward is paid into (QuestCivilization, extracted from the reward sink so both directions provably touch one ledger), and QuestNode_Tribute branches at runtime, which QuestNode_IsSet/IsTrue cannot — they branch during generation, before the quest has run. All or nothing, since a part-payment would need a fraction-to-goodwill rule nobody could source; paid if affordable, since there is no player-input model at this seam; the reprisal applied by the tribute part rather than through QuestPart_Reward, because that part feeds Quest.RewardsSummary and a reprisal is not a reward; and deliberately not scaled by questRewardValueFactor, which is a factor on reward value, not on a cost.",
          "done": true
        }
      ]
    },
    {
      "id": "demography",
      "n": 19,
      "rimworld": "Demography & Lineage",
      "translation": "SimWorld original — RimWorld has no equivalent to port. A colony of twelve never needs generations; a civilization does. Built on the ported age/life-stage clock rather than a compressed one. Citizen tiering (§11.3) is the same idea pushed further: a civilization needs most of its population cheap, not absent — RimWorld's world-pawn precedent (identity/ageing kept, needs/jobs/health frozen off-map) generalised from \"on the map or not\" to \"significant or not\". Migration (demography.migration) closes the module's last open seam: MigrationManager mirrors the birth formula's own shape (base × (0.5 + quality) × a situational factor) for arrivals, where quality is the same per-citizen mood/food signal births already read, plus an era factor — a more advanced settlement has more to offer. Arrivals generate one adult migrant per successful roll and found their household through the existing FamilyManager.FoundHousehold(founder, null, tick) hook (built for exactly this); a settlement's bare Statistical cohort grows by the same closed-form percentage idiom Settlement.GrowStatisticalCohort already uses for births-minus-deaths, never by materialising real Pawns for it. Departures remove a distressed family's living members together from a caller-owned population list — the same list-mutation contract FamilyManager.DemographyTick already uses — but Settlement itself exposes no public way to shrink StatisticalPopulation or remove a citizen (World/** is out of this lane's bounds to add one), so the Settlement-aware overload can only ever floor migration at zero, never make it negative; see MigrationManager's own doc for the stated limitation.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/Pawns — Family · FamilyManager · Pawn_RelationsTracker · DemographyTuning · Pawn_TierTracker · MigrationManager · MigrationTuning",
      "status": "translated",
      "tests": 51,
      "items": [
        {
          "id": "demography.households",
          "label": "Family as the unit of lineage: founders, surname drawn from the shared name banks, members, generation depth, living vs total counts; Pawn_RelationsTracker carries familyId, spouseId, motherId, fatherId",
          "done": true
        },
        {
          "id": "demography.marriage",
          "label": "Marriage founds a new household rather than merging into an existing one — the rule that keeps a civilization from collapsing into one dynasty (Epoch measured 90% in a single household; SimWorld's 120-year regression ends at 0.8% across 284 households)",
          "done": true
        },
        {
          "id": "demography.births",
          "label": "Birth roll per couple per interval, gated by fertility window, minimum birth interval and food security; only the shape of Epoch's formula is reused, every constant is SimWorld's own",
          "done": true
        },
        {
          "id": "demography.lifespan",
          "label": "Death from age as a hidden pre-rolled budget (lifeExpectancy ± spread) with no public accessor — the player never sees how long a citizen has left",
          "done": true
        },
        {
          "id": "demography.modifiers",
          "label": "The budget moves with the civilization: completed MedicineHealth research read at birth lengthens it; starvation intervals spend it and being fed again does not refund it",
          "done": true
        },
        {
          "id": "demography.chronicle",
          "label": "Births, marriages and deaths recorded to the chronicle through the storyteller",
          "done": true
        },
        {
          "id": "demography.lod",
          "label": "Population level-of-detail: PawnTier (Full/Interval/Statistical) and Pawn_TierTracker dispatch ticking by which list a pawn sits on (Normal for Full, Long for Interval and Statistical alike, told apart only by what the coarse tick does there) rather than an if-skip inside Pawn.Tick(); promotion/demotion by significance (attention, role, chronicle mention, relation to someone promoted), never proximity or elapsed time; promotion catch-up brings age (exact, via AgeTickMothballed) and needs (real bulk decay at Interval, cohort sampling at Statistical) current in one step. Measured: Interval ~135 µs/pawn-day, Statistical ~25-36 µs/pawn-day, against Full's ~14,000-34,000 (box-load-dependent; see docs/perf/baseline.md and spec §11.3) — roughly 100-1,000x cheaper per citizen.",
          "done": true
        },
        {
          "id": "demography.migration",
          "label": "Migration and household founding by immigration: arrivals scale with a settlement’s own mood/food quality and its era, founding households through FamilyManager.FoundHousehold’s single-founder hook; departures remove a distressed family from a caller-owned roster, and a settlement’s Statistical cohort moves in both directions (Settlement.RemoveStatisticalPeople), so somewhere worth leaving is as expressible as somewhere worth going. A settlement’s live Citizens list has no departure path — removing a named citizen is the settlement’s own business, and the caller-owned-list overload is what that case is for",
          "done": true
        },
        {
          "id": "demography.migrantrole",
          "label": "A migrant is an ordinary citizen. MigrationManager flagged every arrival with Notify_RoleChanged, which is never cleared and outranks every other reason in the Full-tier ordering — so arrivals accumulated permanently at the top, unbounded, at roughly 25 per settlement per century, filling the budget with people whose only distinction was having arrived",
          "done": true
        }
      ]
    },
    {
      "id": "settlements",
      "n": 20,
      "rimworld": "Settlements & Game Start",
      "translation": "The opening of the game and what a settlement is once it has an interior. Region-then-site founding, deposits derived from terrain, era-weighted site scoring, a settlement that is an entity rather than a def plus a tile, and rival civilizations that emerge from play — World.EmergenceManager — rather than existing at time zero. Every settlement anywhere, including world generation's own, is now a real Settlement founded through SettlementFounder; World.Settlements is typed Settlement, not a WorldObject mix. Closing the last gap in §11.2/§11.3's seam: Settlement.SyncCitizenSpawns places every Full-tier citizen (never Interval, never — by the tiering system's own design — a Statistical one) onto the interior map the instant it is entered, near whatever the settlement has already built, and reconciles that presence on a rare gated cadence after — dropping a citizen who died (off the map and off the roster, so population never keeps counting them) and moving one who leaves Full off the map without anyone tearing the interior itself down, since nothing in this codebase ever discards a generated map once cached. A citizen currently spawned is saved by reference to the one real copy the map's own Thing list already owns, never deep-saved a second time, so a round-tripped save neither duplicates nor loses anyone.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/World (regions, deposits, siting, solo start, Settlement entity, SettlementFounder, EmergenceManager)",
      "status": "ported",
      "tests": 60,
      "items": [
        {
          "id": "settlements.regions",
          "label": "Region partition over the icosphere: contiguous tile groups cut on coastline, ridges and major rivers, each with a name, dominant biome, climate and summarised resource profile",
          "done": true
        },
        {
          "id": "settlements.deposits",
          "label": "Resource and landmark deposits derived from existing terrain — clay in floodplains, flint in chalk lowland, ore in hills, coal in ancient swamp/floodplain low ground, salt at coasts and springs, deep soil in valleys, timber by biome, fords and defensible high ground from elevation",
          "done": true
        },
        {
          "id": "settlements.siting",
          "label": "Site score: hard necessities (fresh water, land that feeds the band, survivable climate) times era-weighted advantages (defensibility, transport, materials, trade position); advisory for the player, decisive for emergent foundings",
          "done": true
        },
        {
          "id": "settlements.trade-position",
          "label": "Trade position scored by inverting the road pather — how many cheap routes would pass through a tile",
          "done": true
        },
        {
          "id": "settlements.founding",
          "label": "Founding band of 20-40 in several households, all Full-tier, with a founding chronicle entry — SettlementFounder, built on FamilyManager.FoundHousehold",
          "done": true
        },
        {
          "id": "settlements.entity",
          "label": "Settlement as an entity: population by tier (real Pawns for Full/Interval, a bare count for Statistical), stores as a def-count ledger, founding tick, name and growth wired to FamilyManager.DemographyTick (plus a closed-form top-up for the Statistical count, which that mechanism cannot run against — see the module's report). \"Works\" (production/workplaces) is not built — out of scope for this pass, tracked separately",
          "done": true
        },
        {
          "id": "settlements.solo",
          "label": "Solo-start world generation: the player's faction alone at time zero",
          "done": true
        },
        {
          "id": "settlements.interior",
          "label": "Settlement interior map generated on entry at settlement scope and persisted after — Settlement.EnterMap, sized by TotalPopulation via a named tuning ladder rather than a flat constant, generated once and cached, Scribe round-tripped (present only for a settlement actually entered)",
          "done": true
        },
        {
          "id": "settlements.emergence",
          "label": "Rival civilizations emerging from the simulation over time rather than being placed at world gen — World.EmergenceManager, ticked from World.WorldTick: a new civilization emerges with a real, live founding band via SettlementFounder.Found (chronicled), and an established civilization grows into a second settlement via the new SettlementFounder.FoundColony (a Statistical-population colony, no live band — routine growth, not an origin story). Every settlement world generation itself places is now a real Settlement too (SettlementFounder.FoundColony, unchronicled backstory), closing World.Settlements' old WorldObject/Settlement mix. Pacing (MTB years between events) is SimWorld's own, pinned by a simulated-span band test, not a literal; determinism (same seed, same emergence history) is tested via the manager's own persisted RandomStream",
          "done": true
        },
        {
          "id": "settlements.coal",
          "label": "Coal as its own terrain-derived deposit (swampiness/rainfall/floodplain low ground, measurably distinct from Ore's hills and mountains), worth ~0 in site scoring before Medieval and the single highest advantage weight by Industrial, and tradeable via Economy.CoalSupply — a settlement without local coal can buy it from the nearest settlement that has it, at a real markup over sitting on your own deposit. CoalSupply now also reads a real Settlement's actual stores (additively, alongside the terrain signal — see the Settlement-entity module's report) rather than inferring access purely structurally",
          "done": true
        },
        {
          "id": "settlements.citizen-presence",
          "label": "A settlement's Full-tier citizens are actually placed on its interior map, closing the gap every other module worked around: Settlement.EnterMap spawns every not-yet-spawned Full citizen (Settlement.SyncCitizenSpawns), placed by GenRadial nearest-first search out from the centroid of whatever the settlement has already built (the map's own centre before anything has), never a corner. Interval never spawns — no jobs, no mind state, spec §11.3's own words — and a Statistical citizen was never eligible to begin with (no live Pawn exists for one). Re-checked on SettlementTuning.CitizenMapSyncIntervalTicks (the same rare-tick bucket SettlementConstructionInitiative already uses): a citizen who died is dropped from Citizens and taken off the map (no Corpse system yet — Health's territory, not this one); a citizen who falls out of Full (a future attention system's demotion) is taken back off the same way, and one promoted back to Full is placed again — the real, reachable form of \"release\" this design has, since nothing anywhere discards an already-generated InteriorMap. Scribe-safe by construction: a spawned citizen is written as a reference to the one real Pawn the map's own deep Thing list already owns, an unspawned one deep-saved here as before, so a round trip neither duplicates nor loses anyone.",
          "done": true
        }
      ]
    },
    {
      "id": "stats",
      "n": 21,
      "rimworld": "Stat System",
      "translation": "Unchanged.",
      "translationState": "decided",
      "module": "src/SimWorld.Core/Stats · Data/Core/Defs (StatDefs, StatCategoryDefs)",
      "status": "ported",
      "tests": 24,
      "items": [
        {
          "id": "stats.pipeline",
          "label": "StatRequest (Thing, or def+stuff with no Thing) -> StatWorker.GetValue: base value (statBases, else defaultBaseValue) -> skill-need factors, then skill-need offsets (system: work.stats) -> trait and hediff-stage offsets -> trait, hediff-stage and stuff factors -> capacity factors -> stat parts -> post-process curve -> min/max clamp",
          "done": true
        },
        {
          "id": "stats.capacityFactors",
          "label": "StatDef.capacityFactors reads the Health module's PawnCapacityDef levels (RestRateMultiplier: BloodPumping/Metabolism/Breathing at weight 0.3 each) so a wounded pawn's stats degrade and recover with the body",
          "done": true
        },
        {
          "id": "stats.retired-workarounds",
          "label": "Pawn.RestRateMultiplier/ImmunityGainSpeed/PainShockThreshold/GlobalLearningFactor/MentalBreakThreshold and Armor.NaturalArmor.ArmorRating now read the pipeline instead of a hard-coded constant or a bare statBases lookup; the two duplicate StatDefOf classes (Crafting, Economy) are retired into one SimWorld.Stats.StatDefOf",
          "done": true
        },
        {
          "id": "stats.content",
          "label": "Core XML: RestRateMultiplier, ImmunityGainSpeed, PainShockThreshold, GlobalLearningFactor, MentalBreakThreshold StatDefs (every default sourced from RimWorld's own vanilla values), BasicsPawn/Basics StatCategoryDefs, Human's MentalBreakThreshold moved from a RaceProperties field to a statBases entry",
          "done": true
        },
        {
          "id": "stats.stuff-quality-parts",
          "label": "Closed properly, not by assertion: CompQuality (Things) gives QualityCategory a home on a live Thing; StatPart_Quality (Stats) is the first concrete StatPart this module ships, wired onto MarketValue's own content (Stats_Economy.xml); Thing.Stuff — plus ThingMaker applying it and StatRequest.For(Thing) reading it — finally gives the stuff factor/offset pass (real and tested since stats.pipeline, previously always fed StuffDef=null for a live Thing) something to read. Tests: tests/SimWorld.Core.Tests/Things/QualityAndStuffTests.cs (filed with CompQuality rather than under Stats, per this pass's test-directory ownership).",
          "done": true
        }
      ]
    },
    {
      "id": "god",
      "n": 22,
      "rimworld": "God Layer & Civilization Translation",
      "translation": "SimWorld's own — RimWorld has no equivalent to port. The re-focus from §10: the player is not a colony overseer but a god directing one civilization, so the player's own verb changes from drafting individuals to issuing standing civilization-scale edicts and reading aggregate rollups. Kept in RimWorld's own declarative shapes throughout even though the concept is new: Def + Worker + Class= polymorphism for EdictDef/EdictWorker (mirroring WorkGiverDef/WorkGiver and ThoughtDef/ThoughtWorker), IExposable for GodManager, a Find service exactly like Find.Storyteller/Find.ResearchManager, and a think-tree job-giver node for the work push rather than a bespoke priority mechanism. The mood consequence reuses the existing situational-thought pipeline (ThoughtWorker_UnderEdict) instead of inventing a second one, so an edict's cost to public mood appears and disappears with activation on its own, the same way every other situational thought already does. Attention, the trigger the citizen tiering was built around, is defined as the god's focus: zero or one settlement the player has open, named across the host seam by its world tile because Settlement.name is not unique. God.AttentionManager applies focus changes immediately and reconciles periodically from GodManager.GodTick, so a settlement founded or a citizen born between focus changes is still placed at the right tier. A second, narrower translation rides with it: the Interval->Statistical settle is by elapsed insignificance (TieringTuning.IntervalSettleTicks, one game year, SimWorld's own — RimWorld has no middle tier to wait in), while promotion stays by significance alone.",
      "translationState": "implemented",
      "module": "src/SimWorld.Core/God · EdictDef · EdictWorker · EdictWorker_ExemptMinors · ThoughtWorker_UnderEdict · GodManager · GodRollup · GodTuning; src/SimWorld.Core/AI/JobGiver_Edicts.cs · WorkGiverScanUtility.cs (shared with JobGiver_Work)",
      "status": "translated",
      "tests": 77,
      "items": [
        {
          "id": "god.edicts",
          "label": "EdictDef: label/description (inherited from Def), prioritizedWork (List<WorkTypeDef>), an optional requiredEra (EraDef) gate read against ResearchManager.CurrentEra, an optional researchFocus (ResearchProjectDef) set as the current project on activation, a Class=-selected workerClass (EdictWorker) defaulting to the declarative base class, and a moodThought (ThoughtDef) granting a situational mood cost to every citizen the edict's AppliesTo(Pawn) reaches. EdictWorker_ExemptMinors ships as the one concrete worker exercising the Class= seam for something a def field alone cannot express (a harsh edict that spares children). Also carries prioritizedConstruction (List<ThingDef>, building.initiative): the same declarative, read-live shape as prioritizedWork, biasing which unmet need SettlementConstructionInitiative fills first rather than a second mechanism; GreatWorksMandate ships it as real content.",
          "done": true
        },
        {
          "id": "god.manager",
          "label": "GodManager (Find.God): active edicts under a slot budget (GodTuning.MaxActiveEdicts, SimWorld's own number, pinned by test rather than trusted as a literal), CanActivate/Activate/Deactivate/IsActive/ActiveEdicts, a Chronicle line on activation and deactivation via Find.Storyteller.RecordChronicle, a self-gated GodTick on the long tick bucket (2000 ticks, GenTicks.TickLongInterval) matching Storyteller.StorytellerTick/FamilyManager.DemographyTick's own idiom, and a full Scribe round trip.",
          "done": true
        },
        {
          "id": "god.thinktree",
          "label": "JobGiver_Edicts inserted into ThinkTrees_Humanlike.xml strictly between JobGiver_DirectedOrder and JobGiver_Work — a queued directed order and every need/mental-state guard above it still outrank a standing edict, and an edict only ever wins a job routine work would otherwise have offered, never pre-empting a need. Scans only the WorkGiverDefs under the active edicts' prioritizedWork types, sharing JobGiver_Work's own nearest-candidate scan via the new WorkGiverScanUtility rather than duplicating it. Never touches Pawn_WorkSettings: deactivating an edict leaves a citizen's own work priorities byte-for-byte unchanged, proved by test.",
          "done": true
        },
        {
          "id": "god.rollup",
          "label": "GodRollup: population (total and by PawnTier), mean mood, mean health, a food/industry readout (mean Need_Food level; mean Construction/Mining/Crafting skill as the industry proxy in the absence of a real production ledger), current era and its research progress. Tier-aware by construction: a Statistical citizen's health contribution is Pawn_TierTracker.SampledHealthFraction, never Pawn_HealthTracker's real hediff-driven summary — proved by test (a severely injured Statistical citizen's real health, ~0.27, never leaks into the rollup, which instead reports the cohort-sampled band). Cached with an explicit dirty flag (Notify_Dirty) and a recompute cadence, not recomputed per tick per reader.",
          "done": true
        },
        {
          "id": "god.content",
          "label": "EdictDefs/Edicts.xml: five edicts spread across the era ladder (HuntersMandate, BreakTheSod, GreatWorksMandate, IronDiscipline, IndustrialMobilization), each with a real requiredEra, prioritizedWork and researchFocus, and its own mood-cost ThoughtDef (ThoughtDefs/Thoughts_Edicts.xml, workerClass ThoughtWorker_UnderEdict). Zero config errors, zero DefOf errors.",
          "done": true
        },
        {
          "id": "god.settlement-population",
          "label": "GodRollup.Recompute(Settlement) and Recompute(IReadOnlyList<Settlement>) read a real Settlement's population by tier: Citizens (real Pawn objects, Full/Interval) walked exactly like the bare-list overload, StatisticalPopulation (a bare count with no Pawn per person) folded in by AccumulateStatisticalCohort — one deterministic RandomStream.RangeSeeded sample per settlement per statistic (mood, food, health, industry skill), seeded from the settlement's own identity and the current tick, reusing Pawn_TierTracker's cohort-sampling idiom rather than a second one, weighted by count in the running mean rather than walked member by member. Counts (TotalPopulation, StatisticalCount) include the cohort exactly; each mean's own doc comment states the weighted-sample choice. New GodTuning.StatisticalIndustrySkillSampleMin/Max (SimWorld's own, not RimWorld-sourced) stand in for the missing per-person SkillRecord. Proved by test: identical output to the bare-list overload when a settlement has no Statistical slice, correct weighting when it dominates, and a 100,000,000-person Statistical settlement recomputing in well under the per-citizen-loop budget (no enumeration proportional to StatisticalPopulation).",
          "done": true
        },
        {
          "id": "god.read-model",
          "label": "God/View: the engine-free contract the host binds to. GodViewSnapshot.Capture() takes one tick's values — civilization rollup, settlements, every edict with an EdictAvailability and a reason in words, chronicle tail and curated moments — and GodCommands issues or rescinds by defName, explaining every refusal in the same wording the snapshot used. Everything crosses the seam as strings and values: the host never holds a Def (so it cannot reach a worker) or a manager (so it cannot mutate state off-tick), and a test asserts that structurally. EdictOption.Availability == Available is pinned to agree with GodManager.CanActivate for every edict at both ends of the era ladder, so the view can never offer something the simulation would then refuse.",
          "done": true
        },
        {
          "id": "god.view",
          "label": "The god view itself — a presentation surface reading the read model below and offering edicts. Needs the Unity host: a headless container has no display, so nothing visual can be built or verified here. The contract it binds to is done (god.read-model); what is left is the surface.",
          "done": false
        },
        {
          "id": "god.era-transitions",
          "label": "GodManager subscribes to ResearchManager.EraReached (constructor for a fresh civilization, ExposeData's PostLoadInit branch for a loaded one, both through the idempotent EnsureSubscribedToEraTransitions so a loaded save can never end up doubly subscribed — proved by test) and reacts on every crossing: EdictDef gained obsoleteEra, the mirror of requiredEra (same minEra/maxEra symmetry as IncidentDef in Director/IncidentDefs.cs, but >= rather than >, since reaching the named era is itself the retirement moment per the brief) — an active edict past its obsoleteEra auto-deactivates with its own \"Edict outgrown\" Chronicle line. Separately, one \"New edicts available\" Chronicle line names every edict the crossing newly unlocks (never one per edict, never written when nothing unlocked). HuntersMandate ships with obsoleteEra=Agrarian as real content proving the mechanism, not just an ad-hoc test def.",
          "done": true
        },
        {
          "id": "god.attention",
          "label": "Attention wired: God.AttentionManager drives Pawn_TierTracker.Notify_AttentionChanged from a settlement focus, GodCommands.FocusSettlement/ClearSettlementFocus is the host's write path, GodViewSnapshot.FocusedSettlementTile reads it back, and unattended citizens settle Interval->Statistical after a year of insignificance",
          "done": true
        },
        {
          "id": "god.fullcap",
          "label": "Full-tier budget with a significance ordering. AttentionBudget seats the TieringTuning.FullTierBudget most significant citizens of the focused settlement at Full and withholds attention from the rest, ordered hasRole > chronicleNamed > relatedToPromoted > attending: the first three are properties of the person and held by few, the fourth is a property of the camera and held by the whole roster, so it yields first. Ties break on thingIDNumber ascending, which is unique, unchanging across a save, and monotone in creation order — so every new citizen sorts below every incumbent and demography can add a thousand people without displacing anyone, which is what makes it thrash-free under growth. A measured century run holds Full flat at the budget from year 80 while the roster passes 1,132",
          "done": true
        },
        {
          "id": "god.opensettlement",
          "label": "GodCommands.OpenSettlement/GenerateSettlementInterior close the seam: the host is told to bind to God/View and never reach into Game, and God/View had no way to generate an interior map at all — so a host could focus a town, hold attention with no map, and have raids resolve mapless forever, with nothing in the seam to do about it. Open focuses then enters, in that order: only a Full-tier citizen is ever placed on an interior and only attention holds an ordinary citizen at Full, so entering first draws an empty town that self-corrects on the next sync sweep — a failure that reads as a rendering glitch. A test proves the order matters by reversing it",
          "done": true
        }
      ]
    },
    {
      "id": "offices",
      "n": 23,
      "rimworld": "No direct equivalent. RoyalTitleDef is granted by an outside faction and has no succession; Precept_Role is about belief and is already ported as IdeoRoleDef.",
      "translation": "SimWorld's own. Work.RoleDef already existed and says what a citizen does; what was missing is the seat — scarcity, a place, a selection rule and succession. An OfficeDef names a Work.RoleDef the way GuildDef already does, so a station is a role plus a seat. Wiring Work.RoleDef straight into Pawn_TierTracker.Notify_RoleChanged would have been wrong: WorkPolicyUtility.ApplyRoleToPopulation sweeps a whole settlement, so 'has a role' would have ranked the entire roster top and collapsed the ordering AttentionBudget spends its budget against. Selection is a pure function of ages, ids and opinions — nothing touches Rand at all, seeded or otherwise, so two runs from one save seat the same citizen. The module is stateless: the holder is the role-carrier, and its whole footprint in a file it does not own is one PostTickers line.",
      "translationState": "recorded",
      "module": "src/SimWorld.Core/Offices",
      "status": "translated",
      "tests": 20,
      "items": [
        {
          "id": "offices.def",
          "label": "OfficeDef, OfficeSelectionWorker and OfficeManager: a seated station that names a Work.RoleDef for what its holder does",
          "done": true
        },
        {
          "id": "offices.steward",
          "label": "Steward, per settlement: candidacy by years lived, selection by summed opinion from a bounded electorate, tenure for life, and the seat always refilled — a town always needs someone to speak for it",
          "done": true
        },
        {
          "id": "offices.elder",
          "label": "Elder, per civilization: eligible only while old enough to remember the founding, so once the last such citizen dies the office retires permanently and needs no saved flag to stay retired",
          "done": true
        },
        {
          "id": "offices.consequence",
          "label": "A holder's work priorities change through the existing SetRole, a situational thought reaches mood and everything downstream, and accession and succession are written for the MomentCurator",
          "done": true
        },
        {
          "id": "offices.edicts",
          "label": "Gating edicts on a seated leader is the strongest remaining consequence and needs GodManager.CanActivate",
          "done": false
        }
      ]
    },
    {
      "id": "weather",
      "n": 24,
      "rimworld": "Verse.WeatherDef, WeatherManager, WeatherDecider, GenTemperature",
      "translation": "Commonalities live on WeatherDef rather than BiomeDef.weatherCommonalities — the relationship is inverted so a biome added later needs no edit to any weather, and no biome file had to be touched. WeatherDef.temperatureOffset is SimWorld's own: RimWorld's swings are GameConditionDefs; that system landed a batch later and plugs into exactly this seam. Rain rolls against fire map-wide on Fire.ComplexCalcsInterval rather than inside each fire's own pass, same rate per fire without editing Fire.cs. Weather exists only where a Map exists — the settlement the player opened — because everything weather moves (a fire, a room, a plant, a mood) only exists on a generated map, so the god layer is deliberately not widened to carry it.",
      "translationState": "recorded",
      "module": "src/SimWorld.Core/Weather",
      "status": "translated",
      "tests": 21,
      "items": [
        {
          "id": "weather.def",
          "label": "WeatherDef and WeatherManager: current and outgoing weather with every rate lerped across the transition, chosen by biome commonality, tile rainfall and current outdoor temperature through the seeded stream",
          "done": true
        },
        {
          "id": "weather.temperature",
          "label": "GenTemperature: seasonal cosine by latitude with the southern hemisphere flipped, a daily sun cycle and the weather offset — a pure function of tick and tile with no randomness. It drives Map.outdoorTemperature, which rooms, plant growth and corpse rot already read",
          "done": true
        },
        {
          "id": "weather.consumers",
          "label": "Wired to what exists: rain extinguishes fire through the path the fire lane wrote and left waiting, ShotReport.factorFromWeather stops defaulting to 1, movement slows in the open, and being rained on is a thought",
          "done": true
        },
        {
          "id": "weather.snow",
          "label": "Snow accumulation, lightning and weather events are left out: there is no snow grid and no weather-event system, and a thunderstorm that is only windy rain would be a name pretending to be a mechanic",
          "done": false
        }
      ]
    },
    {
      "id": "conditions",
      "n": 25,
      "rimworld": "Verse.GameCondition, GameConditionManager, GameConditionDef",
      "translation": "Registration is civilization-scale by default, which is a translation rather than a port: the only IIncidentTarget here is one CivilizationTarget for the whole civilization, and the settlement an incident picks usually has no interior map, so a condition registered per-map would exist only while somebody was watching — fire, find no map, change nothing. The per-map manager is ported and real and parented to the world's, and is where a future settlement-local condition belongs. The condition names its incident rather than the incident naming the condition, inverting RimWorld's relationship so the Director's most shared file needs no new fields. No storm centre, and an unwatched flashstorm burns nothing: the raid lane could resolve abstractly because both sides were priced in combatPower, and there is no comparable currency for how much of a settlement a lightning fire takes.",
      "translationState": "recorded",
      "module": "src/SimWorld.Core/Conditions",
      "status": "translated",
      "tests": 23,
      "items": [
        {
          "id": "conditions.core",
          "label": "GameCondition, GameConditionManager and GameConditionDef, with a manager on the world and one per map whose parent is the world's",
          "done": true
        },
        {
          "id": "conditions.placeholders",
          "label": "HeatWave, ColdSnap and Flashstorm stop being IncidentWorker_Placeholder. They shipped in content, appeared in the storyteller's repertoire, could be selected, fired, and changed nothing",
          "done": true
        },
        {
          "id": "conditions.reuse",
          "label": "Built on what existed rather than beside it: temperature offsets ride the seam WeatherDef.temperatureOffset's own doc predicted, so rooms, plant growth and corpse rot feel a heat wave with no change; flashstorm strikes go through FireUtility so its fires grow, spread, are beaten out and are rained out like any other",
          "done": true
        },
        {
          "id": "conditions.maplocal",
          "label": "Map-scoped registration is a real seam that shipped content does not yet use",
          "done": false
        }
      ]
    }
  ]
}
