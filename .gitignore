# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
.vs/
*.user
*.suo

# Node (docs tooling: markdownlint/mermaid CI, blueprint generator)
node_modules/
npm-debug.log*
tools/blueprint/dist/

# Python (tooling scripts, if any)
__pycache__/
*.pyc
.venv/

# .NET (all bin/obj are redirected here by Directory.Build.props)
/artifacts/
bin/
obj/
*.binlog
TestResults/

# Unity host project (added when there is something to render)
/unity/**/Library/
/unity/**/Temp/
/unity/**/Logs/
/unity/**/UserSettings/
/unity/**/Obj/
/unity/**/Build/
/unity/**/Builds/
/unity/**/*.csproj
/unity/**/*.sln

# simWorld.Host (a separate repo under A:\dev\) references src/SimWorld.Core as a Unity
# local package, which makes its Editor write a .meta sidecar next to every file it
# imports from there. This repo has no legitimate use for .meta files itself.
*.meta

# Build output
/build/
/dist/
*.log
